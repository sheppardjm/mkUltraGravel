---
phase: 05-photo-map-markers
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Photo markers do not cause visible pan or zoom jank on a mid-range mobile device"
    - "Cluster tap-to-zoom works on touch screens"
    - "Thumbnail popups open correctly on mobile tap (no double-tap needed)"
    - "Photo images load in popups without broken image icons (no 404s)"
  artifacts: []
  key_links: []
---

<objective>
Verify that photo markers, clustering, and thumbnail popups work correctly on a real mobile
device with no pan/zoom jank, correct touch behavior, and no broken image references.

Purpose: Success criterion #4 requires "no visible pan or zoom jank on mid-range mobile hardware."
33 markers with image popups are a plausible source of mobile performance issues -- this checkpoint
confirms they are not.

Output: Human-verified confirmation that the photo marker experience is smooth on mobile.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-photo-map-markers/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for mobile testing</name>
  <files></files>
  <action>
    Start the Astro dev server with host flag so it is accessible on the local network:
    ```
    volta run npm run dev -- --host
    ```
    Print the local network URL (e.g., http://192.168.x.x:4321) for the user to open on their
    mobile device.
  </action>
  <verify>Dev server starts and prints a network-accessible URL.</verify>
  <done>Dev server running and accessible on local network.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    33 photo markers with clustering and thumbnail popups on the Leaflet map (Plan 05-01).
  </what-built>
  <how-to-verify>
    Open the network URL on a real mobile device (mid-range Android or iPhone). Test:

    1. **Cluster rendering at overview zoom:** The map loads showing the full route. Photo markers
       should appear as clustered dark circles with count numbers (not 33 overlapping individual markers).

    2. **Tap cluster to zoom:** Tap a cluster icon. The map should zoom in smoothly to show
       sub-clusters or individual markers. No visible jank or stutter.

    3. **Tap individual marker:** At a zoomed-in level where individual cyan square markers are
       visible, tap one. A popup should open showing a thumbnail photo (not a broken image icon).

    4. **Thumbnail loads:** The thumbnail image should load within the popup. Verify it is a real
       photo of the route terrain (not a placeholder or error).

    5. **Tap thumbnail to open full-size:** Tap the thumbnail image in the popup. It should open
       the full-size image in a new tab.

    6. **Pan/zoom smoothness:** Two-finger pan and pinch-zoom around the map while photo markers
       are visible. Movement should be smooth with no visible frame drops or stutter.

    7. **Single-finger scroll past map:** Scroll the page with a single finger. The page should
       scroll past the map without being trapped inside it (gesture handling from Phase 3).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 7 mobile verification checks pass without issues.
</verification>

<success_criteria>
- Human confirms photo markers render, cluster, and uncluster without jank on a real mobile device
- Human confirms thumbnail popups open correctly on tap with real photo images
- Human confirms no scroll-trapping regression from the new markers
</success_criteria>

<output>
After completion, create `.planning/phases/05-photo-map-markers/05-02-SUMMARY.md`
</output>
