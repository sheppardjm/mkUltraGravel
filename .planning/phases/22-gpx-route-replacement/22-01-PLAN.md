---
phase: 22-gpx-route-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/parse-gpx.js
  - public/data/route-data.json
  - public/data/annotations.json
  - public/data/photos.json
  - public/mk-ultra.gpx
  - src/components/ElevationProfile.astro
  - src/pages/index.astro
autonomous: false

must_haves:
  truths:
    - "Map displays a ~100mi route polyline (not the old 80mi track)"
    - "Elevation profile x-axis spans the full route distance without clipping"
    - "Route section displays '100 miles' (not '101 miles' or '80 miles')"
    - "GPX download link serves the new 100mi file"
    - "All 6 sector overlays and 3 KOM overlays land on correct road segments"
  artifacts:
    - path: "public/data/route-data.json"
      provides: "Route track data from 100mi GPX"
      contains: "totalMi.*100"
    - path: "public/data/annotations.json"
      provides: "Sector/KOM/restock annotations resolved against new track"
    - path: "public/data/photos.json"
      provides: "Photo positions resolved against new track"
    - path: "public/mk-ultra.gpx"
      provides: "Downloadable GPX file (100mi version)"
    - path: "scripts/parse-gpx.js"
      provides: "Pipeline source pointing to MK_Ultra.gpx"
      contains: "MK_Ultra.gpx"
    - path: "src/components/ElevationProfile.astro"
      provides: "Dynamic x-axis max from route meta"
      contains: "Math.ceil"
    - path: "src/pages/index.astro"
      provides: "Floor-rounded totalMi display"
      contains: "Math.floor"
  key_links:
    - from: "scripts/parse-gpx.js"
      to: "MK_Ultra.gpx"
      via: "GPX_SOURCE file path"
      pattern: "MK_Ultra\\.gpx"
    - from: "src/components/ElevationProfile.astro"
      to: "public/data/route-data.json"
      via: "fetch and meta.totalMi extraction"
      pattern: "meta\\??\\.totalMi"
    - from: "src/pages/index.astro"
      to: "routeMeta.totalMi"
      via: "Math.floor display"
      pattern: "Math\\.floor.*totalMi"
---

<objective>
Replace the 80mi GPX source with the 100mi GPX, regenerate all pipeline data, and fix two display rounding issues so the site correctly shows the full 100-mile route.

Purpose: The ride has been extended from ~80mi to ~100mi. Every piece of data on the site flows from the GPX source file through the pipeline. Swapping the source and re-running the pipeline cascades the update through route-data.json, annotations.json, photos.json, and the downloadable GPX. Two hardcoded display assumptions (x-axis max and Math.round) must also be fixed.

Output: Regenerated pipeline data, corrected display code, old GPX removed, site builds cleanly with 100mi route.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-gpx-route-replacement/22-RESEARCH.md
@scripts/parse-gpx.js
@src/components/ElevationProfile.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap GPX source and regenerate pipeline data</name>
  <files>
    scripts/parse-gpx.js
    public/data/route-data.json
    public/data/annotations.json
    public/data/photos.json
    public/mk-ultra.gpx
  </files>
  <action>
1. In `scripts/parse-gpx.js` line 29, change:
   `const GPX_SOURCE = path.join(ROOT, 'MK Ultra.gpx');`
   to:
   `const GPX_SOURCE = path.join(ROOT, 'MK_Ultra.gpx');`
   Leave line 30 (`GPX_DEST`) unchanged.

2. Run `npm run data` from the repo root. This executes `node scripts/generate-data.js` which runs all 7 pipeline scripts in sequence. Expect it to complete without errors.

3. Verify pipeline output:
   - `public/data/route-data.json` should contain `"totalMi": 100.71` (approximately) in the meta object
   - `public/data/annotations.json` should contain all 6 sectors and 3 KOMs with lat/lon coordinates
   - `public/data/photos.json` should contain 53 photo entries with lat/lon coordinates
   - `public/mk-ultra.gpx` should be the new 100mi file (file size ~237KB, matching MK_Ultra.gpx)

4. Remove the old GPX file from git tracking:
   `git rm "MK Ultra.gpx"`
   This removes the old 98.23mi Strava file. Git history preserves it if ever needed.

IMPORTANT: Do NOT modify `resolve-annotations.js` or any annotation mile markers. Research confirmed all 6 sector and 3 KOM coordinates are numerically identical on the new GPX (all fall below mi 84.15, within shared track geometry). Do NOT run individual pipeline scripts -- always use `npm run data` to ensure correct ordering.
  </action>
  <verify>
- `node -e "const d = require('./public/data/route-data.json'); console.log('totalMi:', d.meta.totalMi, 'trackpoints:', d.meta.trackpoints)"` outputs totalMi ~100.71 and trackpoints 2779
- `ls -la public/mk-ultra.gpx` shows file size ~237KB (matching MK_Ultra.gpx, not the old ~231KB)
- `git status` shows "MK Ultra.gpx" as deleted, and public/data/*.json + public/mk-ultra.gpx as modified
- `node -e "const a = require('./public/data/annotations.json'); console.log('sectors:', a.sectors.length, 'kom:', a.kom.length, 'restock:', a.restock.length)"` outputs sectors: 6, kom: 3, restock: (whatever the current count is)
  </verify>
  <done>Pipeline reads from MK_Ultra.gpx, all three JSON files regenerated with 100mi data, downloadable GPX updated, old GPX removed from git tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Fix elevation profile x-axis and route distance display</name>
  <files>
    src/components/ElevationProfile.astro
    src/pages/index.astro
  </files>
  <action>
**ElevationProfile.astro -- dynamic x-axis max:**

1. Find the current fetch pattern (around line 45-46):
   ```javascript
   const [routeData, annotations] = await Promise.all([
     fetch('/data/route-data.json').then(r => r.json()).then(d => d.track ?? d),
     fetch('/data/annotations.json').then(r => r.json())
   ]);
   ```

2. Replace with a pattern that retains the meta object:
   ```javascript
   const [routeJson, annotations] = await Promise.all([
     fetch('/data/route-data.json').then(r => r.json()),
     fetch('/data/annotations.json').then(r => r.json())
   ]);
   const routeData = routeJson.track ?? routeJson;
   const totalMi = routeJson.meta?.totalMi ?? routeData[routeData.length - 1]?.mi ?? 110;
   ```

3. Find the x-axis config (around line 195-196):
   ```javascript
   max: 100,  // forward-compatible with 100mi route
   ```
   Replace with:
   ```javascript
   max: Math.ceil(totalMi),
   ```

4. IMPORTANT: After this change, every reference to `routeData` elsewhere in the file must still work. The variable name `routeData` is preserved (it is extracted from `routeJson`). Scan the file to confirm `routeData` is used for chart data mapping and nothing references the old destructured variable name directly.

**index.astro -- Math.floor fix:**

5. Find line 248:
   ```astro
   {Math.round(routeMeta.totalMi)} miles &mdash;
   ```
   Replace `Math.round` with `Math.floor`:
   ```astro
   {Math.floor(routeMeta.totalMi)} miles &mdash;
   ```

Do NOT use `Math.round` -- 100.71 rounds to 101, which is wrong. `Math.floor(100.71)` gives 100, which matches the ride's marketed distance.
  </action>
  <verify>
- `grep -n 'Math.ceil' src/components/ElevationProfile.astro` shows the dynamic x-axis max
- `grep -n 'Math.floor.*totalMi' src/pages/index.astro` shows the floor-rounded display
- `grep -n 'max: 100' src/components/ElevationProfile.astro` returns NO matches (hardcoded max removed)
- `grep -n 'Math.round.*totalMi' src/pages/index.astro` returns NO matches (Math.round removed)
- `npm run build` completes without errors
  </verify>
  <done>Elevation profile x-axis dynamically spans the full route distance (Math.ceil of totalMi). Route section displays "100 miles" (not "101 miles"). Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete GPX route replacement: 100mi GPX source swapped, pipeline regenerated all data (route-data.json, annotations.json, photos.json, downloadable GPX), elevation profile x-axis now dynamic, route distance displays "100 miles", old GPX removed.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:4321 in a browser.

2. **Route polyline:** Scroll to the map. The route should display the full 100mi track. If you know the route, check that the new extension (past the old turnaround) is visible.

3. **Route distance:** The route section heading should read "100 miles" with updated elevation gain (~3,595 ft, not the old ~3,189 ft).

4. **Elevation profile:** Hover/scroll the elevation chart. The x-axis should extend to ~101 mi (Math.ceil of 100.71). The elevation line should NOT end abruptly before the right edge of the chart.

5. **Sector/KOM overlays:** Click a few sector and KOM cards. Verify the map zooms to the correct road segments (they should be on the route, not offset). Check at zoom 14+ for accuracy.

6. **GPX download:** Scroll to the bottom section. Click the GPX download link. The downloaded file should be the new 100mi route (check file size ~237KB, or open in a GPX viewer).

7. **No "80 mile" or "101 miles" text:** Scan the page for any references to 80 miles or 101 miles. There should be none.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
- Pipeline generates correct data: `node -e "const d = require('./public/data/route-data.json'); console.log(d.meta)"` shows totalMi ~100.71, trackpoints 2779
- No hardcoded x-axis max: `grep 'max: 100' src/components/ElevationProfile.astro` returns nothing
- No Math.round on totalMi: `grep 'Math.round.*totalMi' src/pages/index.astro` returns nothing
- Old GPX removed: `git status` shows "MK Ultra.gpx" deleted
- New GPX tracked: `MK_Ultra.gpx` exists and is staged
- Build succeeds: `npm run build` exits 0
</verification>

<success_criteria>
1. Map displays the 100mi route polyline
2. All 6 sector and 3 KOM overlays land on correct road segments at zoom 14+
3. Elevation profile x-axis spans full route distance (no clipping at mi 100)
4. Route section displays "100 miles" (not "101 miles")
5. GPX download serves the new 100mi file
6. No "80 mile" / "80-mile" text anywhere on site
7. `npm run build` succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-gpx-route-replacement/22-01-SUMMARY.md`
</output>
