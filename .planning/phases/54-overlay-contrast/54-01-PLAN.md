---
phase: 54-overlay-contrast
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EscherLizards.astro
autonomous: false

must_haves:
  truths:
    - "Light text (white, muted, accent green) remains legible over the Escher lizard tessellation in the #sectors section at every scroll position"
    - "The Escher lizard texture is still perceptible as a background element in #sectors — not invisible"
  artifacts:
    - path: "src/components/EscherLizards.astro"
      provides: "Reduced opacity on .escher-lizards class"
      contains: "opacity: 0.07"
  key_links:
    - from: "src/components/EscherLizards.astro"
      to: "#sectors section in index.astro"
      via: "absolute-positioned SVG behind z-10 content"
      pattern: "opacity:\\s*0\\.07"
---

<objective>
Reduce the EscherLizards tessellation opacity from 0.12 to 0.07 so light-colored text in the #sectors section maintains readable contrast against the dark background.

Purpose: The `.escher-lizards` SVG in `#sectors` uses `opacity: 0.12` with `mix-blend-mode: lighten`, which locally brightens the dark background enough to degrade contrast for muted text. Reducing opacity to 0.07 preserves the visual texture while restoring adequate text-background contrast.

Output: Updated EscherLizards.astro with reduced opacity; visual verification that text is legible and texture remains perceptible.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/EscherLizards.astro
@src/styles/global.css (lines 162-168 — .tone-image class, for reference only — do NOT modify)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce EscherLizards opacity</name>
  <files>src/components/EscherLizards.astro</files>
  <action>
In `src/components/EscherLizards.astro`, in the `<style>` block, change the `.escher-lizards` class `opacity` from `0.12` to `0.07`.

Leave all other properties unchanged:
- `mix-blend-mode: lighten` — keep as-is
- `filter: grayscale(100%) contrast(1.3)` — keep as-is
- `position: absolute`, `inset: 0`, etc. — keep as-is

Do NOT modify:
- `.escher-overlay` in `global.css` (the fixed Penrose tile at opacity 0.05 — different component)
- `.tone-image` in `global.css` (shared across many sections — out of scope)
- `.lizard-bg` in `LizardBackground.astro` (fixed layer at opacity 0.04 — different component)
  </action>
  <verify>
Run `grep 'opacity' src/components/EscherLizards.astro` and confirm output shows `opacity: 0.07;`
Run `npm run build` and confirm no build errors.
  </verify>
  <done>`.escher-lizards` opacity is 0.07 in EscherLizards.astro. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reduced EscherLizards opacity from 0.12 to 0.07 to improve text contrast in the #sectors section while preserving the lizard tessellation texture.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the site in a browser
2. Scroll to the **#sectors** section (Gravel Sectors heading)
3. Verify these two conditions:
   - **Legibility:** All text in the sectors section — the heading, sector card labels, muted text — is clearly readable against the dark background. Pay attention to lighter/muted text colors.
   - **Texture visible:** The Escher lizard tessellation pattern is still faintly visible as a background texture behind the sector cards. It should be subtle but perceptible — not invisible.
4. Scroll through the entire page to confirm no other sections have text legibility issues introduced by this change (this change only affects #sectors, but verify nothing unexpected happened).

If text is still hard to read: the opacity may need to go lower (0.05).
If the texture has disappeared: the opacity may need to come back up (0.08–0.09).
  </how-to-verify>
  <resume-signal>Type "approved" if both conditions pass, or describe which condition fails and what adjustment is needed.</resume-signal>
</task>

</tasks>

<verification>
1. `grep 'opacity' src/components/EscherLizards.astro` shows `0.07`
2. `npm run build` succeeds with no errors
3. Visual confirmation that text is legible and texture is perceptible in #sectors
</verification>

<success_criteria>
- EscherLizards opacity reduced from 0.12 to 0.07
- Light text in #sectors section is clearly readable at all scroll positions
- Escher lizard tessellation is still faintly visible as background texture
- No build errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/54-overlay-contrast/54-01-SUMMARY.md`
</output>
