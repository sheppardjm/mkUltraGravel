---
phase: 53-card-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GravelSectors.astro
  - src/components/KomSegments.astro
  - scripts/assign-card-photos.js
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "CLASSIFIED badge renders fully visible above every segment card — no clipping by overflow-hidden or stacking context"
    - "Card photos appear sharp on 1440px+ viewports (1200x675 source files, not 600x338)"
    - "Sectors section content stops growing at max-w-6xl on ultrawide viewports (2560px+)"
  artifacts:
    - path: "src/components/GravelSectors.astro"
      provides: "Restructured card DOM — overflow-hidden scoped to media container only"
      contains: "width=\"1200\""
    - path: "src/components/KomSegments.astro"
      provides: "Restructured card DOM — overflow-hidden scoped to media container only"
      contains: "width=\"1200\""
    - path: "scripts/assign-card-photos.js"
      provides: "Card crops generated at 1200x675"
      contains: "resize(1200, 675"
    - path: "src/pages/index.astro"
      provides: "Max-width constraint on sectors content"
      contains: "max-w-6xl"
  key_links:
    - from: "scripts/assign-card-photos.js"
      to: "public/images/cards/*.webp"
      via: "sharp resize pipeline"
      pattern: "resize\\(1200, 675"
    - from: "GravelSectors.astro / KomSegments.astro"
      to: "public/images/cards/*.webp"
      via: "img width/height attributes"
      pattern: "width=\"1200\" height=\"675\""
---

<objective>
Fix three card display issues on wide viewports: classified badge clipped by overflow-hidden, card photos too low-res (600x338 vs. needed 1200x675), and unbounded card width on ultrawide screens.

Purpose: Segment cards are the primary content showcasing gravel sectors and KOM climbs — they must look polished on the wide-screen viewports where most desktop visitors see them.
Output: Restructured card DOM in both components, regenerated higher-res card crops, and max-width constraint on sectors section.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-card-display/53-RESEARCH.md
@src/components/GravelSectors.astro
@src/components/KomSegments.astro
@scripts/assign-card-photos.js
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix classified badge clipping and update card photo dimensions</name>
  <files>src/components/GravelSectors.astro, src/components/KomSegments.astro</files>
  <action>
In BOTH GravelSectors.astro and KomSegments.astro, restructure the card DOM so `overflow-hidden` only wraps the media container (image + tone overlay), not the entire card body. The content div must be a sibling of the media container, not nested inside the overflow-hidden div.

**GravelSectors.astro** — for each card in the `sectors.map()`:

Current inner structure (lines 22-73):
```
<div class={`overflow-hidden${i < 2 ? ' relative' : ''}`}>
  <img ... />           <!-- card photo -->
  {tone image}          <!-- conditional tone overlay -->
  <div class={`p-4${i < 2 ? ' relative z-10' : ''}`}>
    <!-- all card content -->
  </div>
</div>
```

Change to:
```
<div class={`overflow-hidden${i < 2 ? ' relative' : ''}`}>
  <img ... />           <!-- card photo -->
  {tone image}          <!-- conditional tone overlay -->
</div>
<div class="p-4">
  <!-- all card content (heading, stars, stats, Strava link) -->
</div>
```

Key changes:
1. Close the `overflow-hidden` div after the tone image (before the content div)
2. Move the `<div class="p-4 ...">` (and all its children) to be a direct child of the outer `.classified-border` div
3. Remove `relative z-10` from the content div — no longer needed since content is outside the overflow-hidden container that holds the tone-image
4. Update `<img>` width/height attributes from `width="600" height="338"` to `width="1200" height="675"`

**KomSegments.astro** — identical restructure pattern:
1. Close `overflow-hidden` div after tone image
2. Move `<div class="p-4 ...">` outside overflow-hidden
3. Remove `relative z-10` from content div
4. Update `<img>` width/height from `width="600" height="338"` to `width="1200" height="675"`

Do NOT change: the outer `.classified-border` wrapper, the `card-hover` class, `min-h-[280px]`, `data-reveal`, animation-delay, or isolation styles. These stay exactly as-is.
  </action>
  <verify>
Run `npx astro check` (or `npm run build`) — zero errors, zero warnings from the restructured components.
Grep both files to confirm:
- No `<img` tag has `width="600"` — all should be `width="1200"`
- The string `overflow-hidden` appears only on the media wrapper div, not wrapping content
- `relative z-10` does NOT appear on any `p-4` content div
  </verify>
  <done>
Both GravelSectors.astro and KomSegments.astro have overflow-hidden scoped to media container only, content div is a sibling outside overflow-hidden, and img tags declare width="1200" height="675".
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate card crops at 1200x675</name>
  <files>scripts/assign-card-photos.js</files>
  <action>
In `scripts/assign-card-photos.js`:

1. Update the file header comment (line 14) from `600x338` to `1200x675`:
   - Line 6: `"generates 600x338 WebP card crops"` → `"generates 1200x675 WebP card crops"`
   - Line 14: `"Card crops written to: public/images/cards/{basename}.webp (600x338, q80)"` → `"Card crops written to: public/images/cards/{basename}.webp (1200x675, q80)"`

2. Update the resize call (line 102-103):
   - Change `.resize(600, 338, { fit: 'cover', position: 'attention' })` to `.resize(1200, 675, { fit: 'cover', position: 'attention' })`

3. Delete all existing card crops so the skip logic doesn't prevent regeneration:
   ```bash
   rm -f public/images/cards/*.webp
   ```

4. Run the card photo assignment script to regenerate:
   ```bash
   node scripts/assign-card-photos.js
   ```

5. Verify the generated cards are 1200x675:
   ```bash
   file public/images/cards/*.webp | head -5
   ```
   Each file should report dimensions of 1200x675.
  </action>
  <verify>
Run: `file public/images/cards/*.webp | head -5` — all files should show 1200x675 dimensions.
Run: `ls public/images/cards/*.webp | wc -l` — should show the same count as before (expect ~10-16 files).
Run: `grep -n 'resize' scripts/assign-card-photos.js` — should show `resize(1200, 675, ...)`.
  </verify>
  <done>
All card WebP files in public/images/cards/ are 1200x675px. The assign-card-photos.js script generates 1200x675 crops for all future runs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add max-width constraint to sectors section</name>
  <files>src/pages/index.astro</files>
  <action>
In `src/pages/index.astro`, find the sectors section content wrapper (the `<div class="relative z-10">` inside `<section id="sectors">`).

Current (around line 290):
```html
<div class="relative z-10">
```

Change to:
```html
<div class="relative z-10 max-w-6xl mx-auto">
```

This caps the entire sectors section content (heading, grinduro explainer, gravel cards grid, KOM cards, restock points) at `max-w-6xl` (72rem = 1152px) and centers it horizontally. This prevents cards from stretching across ultrawide viewports while keeping the layout balanced between gravel and KOM columns.

Do NOT touch the parent `<section id="sectors">` — its `overflow-hidden`, `px-4`, `py-16`, etc. remain as-is.
Do NOT add max-width to the individual column divs — the section-level constraint is the correct approach per the research.
  </action>
  <verify>
Grep `index.astro` for `max-w-6xl` — should appear exactly once, on the z-10 wrapper inside #sectors.
Run `npm run build` — build succeeds with no errors.
  </verify>
  <done>
Sectors section content wrapper has `max-w-6xl mx-auto`, constraining the grid to 1152px max width on ultrawide viewports.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -n 'width="600"' src/components/GravelSectors.astro src/components/KomSegments.astro` returns no matches (all updated to 1200)
3. `grep -n 'overflow-hidden' src/components/GravelSectors.astro src/components/KomSegments.astro` shows overflow-hidden only on media wrapper lines, not wrapping content
4. `file public/images/cards/*.webp` shows all files at 1200x675
5. `grep 'max-w-6xl' src/pages/index.astro` shows the constraint on the z-10 wrapper
6. `grep 'resize(1200, 675' scripts/assign-card-photos.js` confirms the script update
</verification>

<success_criteria>
- CLASSIFIED badge pseudo-element is no longer clipped by any overflow-hidden ancestor on all 10 segment cards
- Card photos are generated at 1200x675px (2x resolution for sharp rendering on 1440px+ viewports)
- Sectors section content constrained to max-w-6xl (1152px) on ultrawide viewports
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/53-card-display/53-01-SUMMARY.md`
</output>
