---
phase: 56-seo-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/results.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Homepage title is 50-60 characters"
    - "Homepage description is 110-160 characters"
    - "Results page title is 50-60 characters"
    - "Results page description is 110-160 characters"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Default title and description for homepage"
      contains: "100 Miles of Upper Peninsula Gravel"
    - path: "src/pages/results.astro"
      provides: "Results page title and description"
      contains: "Race Results & Leaderboards"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "og:title and og:description meta tags"
      via: "Astro props passed to meta tags in <head>"
      pattern: "content=.title."
---

<objective>
Lengthen the homepage and results page title/description strings to meet optimal social sharing thresholds (title: 50-60 chars, description: 110-160 chars).

Purpose: UAT found both pages have titles and descriptions below the optimal character length thresholds recommended by social share debuggers. These short strings reduce click-through from social previews.

Output: Updated default title/description in BaseLayout.astro and explicit title/description in results.astro.

Note: UAT gap 1 (sitemap missing results page) is NOT a code bug -- the local build is correct. A fresh Netlify deploy will resolve the stale sitemap. No code changes needed for that gap.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/56-seo-foundation/v10.5-UAT.md
@src/layouts/BaseLayout.astro
@src/pages/results.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lengthen homepage and results page title/description strings</name>
  <files>src/layouts/BaseLayout.astro, src/pages/results.astro</files>
  <action>
In src/layouts/BaseLayout.astro, update the default prop values on lines 14-15:

Change title default from:
  "MK Ultra Gravel — June 7, 2026"
to:
  "MK Ultra Gravel — 100 Miles of Upper Peninsula Gravel"
(53 chars -- within 50-60 optimal range)

Change description default from:
  "100 miles of rowdy, technical gravel through Michigan's Upper Peninsula. Free ride. Mass start. No mercy."
to:
  "Rowdy, technical gravel through Michigan's Upper Peninsula. 100 miles. Free ride. Mass start. No mercy. June 7, 2026 from Marquette, MI."
(136 chars -- within 110-160 optimal range)

Also update the structuredData.description value (around line 50) to match the new description string so the JSON-LD schema stays consistent with the meta description.

In src/pages/results.astro, update the BaseLayout props on lines 6-7:

Change title from:
  "Results — MK Ultra Gravel"
to:
  "Race Results & Leaderboards — MK Ultra Gravel 2026"
(50 chars -- within 50-60 optimal range)

Change description from:
  "MK Ultra Gravel results and leaderboards are hosted on ironpineomnium.com."
to:
  "Full results, sector times, KOM segments, and leaderboards for all editions of MK Ultra Gravel — hosted on Iron & Pine Omnium."
(126 chars -- within 110-160 optimal range)

Do NOT change any other code in either file. Only the four string values (plus the structuredData.description to match).
  </action>
  <verify>
Run these commands to verify character counts:

```bash
# Extract and count homepage title default
python3 -c "
import re
with open('src/layouts/BaseLayout.astro') as f:
    content = f.read()
m = re.search(r'title\s*=\s*\"([^\"]+)\"', content)
if m:
    t = m.group(1)
    print(f'Homepage title: {len(t)} chars: {t}')
    assert 50 <= len(t) <= 60, f'FAIL: {len(t)} not in 50-60'
    print('PASS')
"

# Extract and count homepage description default
python3 -c "
import re
with open('src/layouts/BaseLayout.astro') as f:
    content = f.read()
m = re.search(r'description\s*=\s*\"([^\"]+)\"', content)
if m:
    d = m.group(1)
    print(f'Homepage desc: {len(d)} chars')
    assert 110 <= len(d) <= 160, f'FAIL: {len(d)} not in 110-160'
    print('PASS')
"

# Verify the Astro build succeeds
npm run build
```
  </verify>
  <done>
  - Homepage title is 53 chars (within 50-60 optimal)
  - Homepage description is 136 chars (within 110-160 optimal)
  - Results title is 50 chars (within 50-60 optimal)
  - Results description is 126 chars (within 110-160 optimal)
  - structuredData.description matches the new homepage description
  - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Homepage title default is 50-60 characters
3. Homepage description default is 110-160 characters
4. Results page title is 50-60 characters
5. Results page description is 110-160 characters
6. JSON-LD structuredData.description matches the new homepage meta description
</verification>

<success_criteria>
All four title/description strings fall within their optimal character ranges. Build succeeds. No other code changes introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/56-seo-foundation/56-02-SUMMARY.md`
</output>
