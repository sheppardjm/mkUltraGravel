---
phase: 07-hero-event-info-ctas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EventInfoBlock.astro
autonomous: true

must_haves:
  truths:
    - "Event format (mass start, not a race) is displayed"
    - "Great Lakes Recovery Centers donation info is displayed with $10 suggested amount and enough context to explain the cause"
    - "GPX download link has href='/mk-ultra.gpx' and download='mk-ultra-gravel-2026.gpx' attribute"
    - "The GPX download link is a visible, styled element (not hidden or tiny)"
    - "All content renders as static HTML at build time — no client-side JavaScript needed"
  artifacts:
    - path: "src/components/EventInfoBlock.astro"
      provides: "Event format, donation info, GLRC explanation, GPX download link"
      contains: "download=\"mk-ultra-gravel-2026.gpx\""
  key_links:
    - from: "src/components/EventInfoBlock.astro"
      to: "/mk-ultra.gpx"
      via: "HTML anchor with download attribute"
      pattern: "href=\"/mk-ultra.gpx\""
---

<objective>
Create the EventInfoBlock.astro component containing the event format description, Great Lakes
Recovery Centers donation information, and GPX file download link.

Purpose: EVENT-02 (donation info) and MAP-08 (GPX download) — riders need to understand the
event format, the charitable cause, and download the route file. This component fills the
existing #info section placeholder.

Output: EventInfoBlock.astro — a static Astro component with event details, donation info,
and a GPX download link styled to match the dark brutalist design system.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hero-event-info-ctas/07-RESEARCH.md

@src/styles/global.css
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventInfoBlock.astro component</name>
  <files>src/components/EventInfoBlock.astro</files>
  <action>
Create `src/components/EventInfoBlock.astro` with the following structure. This is a purely
static component — no frontmatter logic needed, no data loading, no client-side JS.

**Frontmatter:**
```astro
---
// Placeholder — confirm with event director before launch
const BIKEREG_URL = 'PENDING — confirm with event director';
---
```

**Template structure — three content blocks in a vertical stack:**

Wrap everything in a `<div class="space-y-10">`.

**Block 1: Event Format**
```html
<div>
  <h3 class="text-xl mb-4 text-accent-white">The Format</h3>
  <div class="space-y-3 text-text-body text-sm leading-relaxed">
    <p>Mass start. Not a race — ride your own ride.
       No sag wagon, no course marshals, no excuses.</p>
    <p>Self-supported. Carry what you need between restock points.
       The route passes through remote two-track and forest roads
       where cell service is a rumor.</p>
  </div>
</div>
```

**Block 2: Donation / Great Lakes Recovery Centers**
```html
<div>
  <h3 class="text-xl mb-4 text-accent-white">The Cause</h3>
  <div class="space-y-3 text-text-body text-sm leading-relaxed">
    <p>MK Ultra Gravel is free to ride. We suggest a
       <strong class="text-accent-green">$10 donation</strong> to
       <strong class="text-accent-white">Great Lakes Recovery Centers</strong>.</p>
    <p>GLRC provides substance abuse and mental health services across
       Michigan's Upper Peninsula — crisis intervention, residential treatment,
       and outpatient counseling for communities that need it most.</p>
    <p class="text-text-muted text-xs">
      Donate at registration or on ride day. Every dollar stays local.
    </p>
  </div>
</div>
```

**Block 3: GPX Download**
```html
<div>
  <h3 class="text-xl mb-4 text-accent-white">The Route File</h3>
  <p class="text-text-body text-sm mb-4">
    Load the GPX into your head unit before you show up.
    Navigation is on you.
  </p>
  <a
    href="/mk-ultra.gpx"
    download="mk-ultra-gravel-2026.gpx"
    class="inline-block border border-accent-green text-accent-green
           px-6 py-3 text-sm uppercase tracking-widest
           hover:bg-accent-green hover:text-bg-base transition-colors"
  >
    Download GPX
  </a>
</div>
```

IMPORTANT: The `download` attribute sets the saved filename to `mk-ultra-gravel-2026.gpx` even
though the file is served from `/mk-ultra.gpx`. This only works for same-origin (which it is).
IMPORTANT: The BIKEREG_URL constant is defined but NOT used in this component — it's here for
reference. The CTA buttons are placed in index.astro (Plan 03), not in the info block.
IMPORTANT: Do NOT add a `<script>` tag — this component is 100% static HTML.
IMPORTANT: Headings (h3) automatically get `font-family: var(--font-display)` from the base layer.
IMPORTANT: Anchor default styling is already green (from global.css base layer `a` rule), but
the download button uses explicit classes to override the default underline style.
  </action>
  <verify>
- File exists: `ls src/components/EventInfoBlock.astro`
- Has GPX download link: `grep 'download="mk-ultra-gravel-2026.gpx"' src/components/EventInfoBlock.astro` returns match
- Has correct href: `grep 'href="/mk-ultra.gpx"' src/components/EventInfoBlock.astro` returns match
- Has GLRC reference: `grep 'Great Lakes Recovery Centers' src/components/EventInfoBlock.astro` returns match
- Has donation amount: `grep '\$10' src/components/EventInfoBlock.astro` returns match
- No script tag: `grep '<script>' src/components/EventInfoBlock.astro` returns 0 matches
- Has BIKEREG_URL placeholder: `grep 'PENDING' src/components/EventInfoBlock.astro` returns match
  </verify>
  <done>
EventInfoBlock.astro exists with three content blocks: event format description, Great Lakes
Recovery Centers donation info with $10 suggested amount, and a styled GPX download link with
the correct `download="mk-ultra-gravel-2026.gpx"` attribute. All content is static HTML. The
BIKEREG_URL placeholder is defined for future CTA wiring.
  </done>
</task>

</tasks>

<verification>
After task completes:

1. `grep 'download="mk-ultra-gravel-2026.gpx"' src/components/EventInfoBlock.astro` — 1 match
2. `grep 'href="/mk-ultra.gpx"' src/components/EventInfoBlock.astro` — 1 match
3. `grep 'Great Lakes Recovery Centers' src/components/EventInfoBlock.astro` — at least 1 match
4. `grep '\$10' src/components/EventInfoBlock.astro` — at least 1 match
5. `grep '<script>' src/components/EventInfoBlock.astro` — 0 matches
6. `npx astro build` succeeds (component not yet imported — confirming no syntax errors if
   temporarily imported for validation)
</verification>

<success_criteria>
- EventInfoBlock.astro contains event format, donation info, and GPX download
- Great Lakes Recovery Centers is named with enough context about their mission
- $10 suggested donation is clearly stated
- GPX download link has correct href and download attribute
- Component is 100% static — no client-side JavaScript
- BikeReg URL placeholder is clearly marked as PENDING
</success_criteria>

<output>
After completion, create `.planning/phases/07-hero-event-info-ctas/07-02-SUMMARY.md`
</output>
