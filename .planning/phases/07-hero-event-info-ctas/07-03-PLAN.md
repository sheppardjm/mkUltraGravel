---
phase: 07-hero-event-info-ctas
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/pages/index.astro
autonomous: false

must_haves:
  truths:
    - "The event date (June 7, 2026), start location (Marquette Fire Bell), distance, and cost (free / $10 suggested donation) are visible in the hero section"
    - "A BikeReg registration CTA button appears in the hero section (above the fold)"
    - "A second BikeReg CTA button appears immediately below the #route section (below the map)"
    - "The countdown timer is visible in the hero section and actively counting down"
    - "A hero background preload link is in the HTML <head> via the named slot"
    - "The #info section contains EventInfoBlock with donation info and GPX download"
    - "The Phase 7 placeholder text in #info is removed"
    - "Both CTA buttons use the BIKEREG_URL placeholder constant (clearly marked as PENDING)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete page with hero, countdown, CTAs, info block, preload"
      contains: "CountdownTimer"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/CountdownTimer.astro"
      via: "Astro component import"
      pattern: "import CountdownTimer"
    - from: "src/pages/index.astro"
      to: "src/components/EventInfoBlock.astro"
      via: "Astro component import"
      pattern: "import EventInfoBlock"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "Named slot head for preload link"
      pattern: "slot=\"head\""
---

<objective>
Wire CountdownTimer and EventInfoBlock into index.astro: rebuild the hero section with full
event details and an above-fold CTA, fill the #info section, add a second CTA below the map,
and inject the hero background preload via the named head slot.

Purpose: This is the integration plan that makes all Phase 7 content visible on the page.
EVENT-01 (date/location/cost), EVENT-03 (dual CTAs), EVENT-04 (countdown timer), EVENT-02
(donation info), and MAP-08 (GPX download) all become functional after this plan.

Output: Updated index.astro with hero section rebuild, countdown timer, dual CTAs, info
section filled, and hero image preload in head.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hero-event-info-ctas/07-RESEARCH.md
@.planning/phases/07-hero-event-info-ctas/07-01-SUMMARY.md
@.planning/phases/07-hero-event-info-ctas/07-02-SUMMARY.md

@src/pages/index.astro
@src/layouts/BaseLayout.astro
@src/styles/global.css
@src/components/CountdownTimer.astro
@src/components/EventInfoBlock.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild hero section and wire all Phase 7 components into index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
Update `src/pages/index.astro` with the following changes. This is a significant update to
the page but all other sections (#route, #sectors, #photos) remain UNCHANGED.

**Define the BikeReg URL constant in the frontmatter:**
```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import CountdownTimer from "../components/CountdownTimer.astro";
import EventInfoBlock from "../components/EventInfoBlock.astro";
import RouteMap from "../components/RouteMap.astro";
import ElevationProfile from "../components/ElevationProfile.astro";
import GravelSectors from "../components/GravelSectors.astro";
import KomSegments from "../components/KomSegments.astro";
import RestockPoints from "../components/RestockPoints.astro";

// Placeholder — confirm with event director before launch
const BIKEREG_URL = 'PENDING — confirm with event director';
---
```

**Add preload link via named head slot.** Inside `<BaseLayout>`, BEFORE `<main>`, add:
```html
<link
  rel="preload"
  href="/tone/CIA-MKULTRA-IG_Page_01.jpg"
  as="image"
  slot="head"
/>
```
This uses the named slot from Plan 07-01 to inject the preload into `<head>`.

**Rebuild the #hero section.** Replace the entire `<section id="hero">` with:
```html
<section id="hero" class="relative min-h-screen flex items-center justify-center px-4 overflow-hidden">
  <img
    src="/tone/CIA-MKULTRA-IG_Page_01.jpg"
    alt=""
    class="tone-image inset-0 w-full h-full object-cover"
    loading="eager"
  />
  <div class="relative z-10 text-center max-w-3xl">
    <p class="stamp mb-6">Classification: Ultra</p>
    <h1 class="text-5xl md:text-8xl tracking-tight mb-4">MK Ultra Gravel</h1>
    <p class="text-text-muted text-lg md:text-xl mb-2">
      June 7, 2026 &mdash; Marquette, MI
    </p>
    <p class="text-text-muted text-sm mb-6">
      Marquette Fire Bell &mdash; 80 miles &mdash; Free
    </p>

    <div class="mb-8">
      <CountdownTimer />
    </div>

    <a
      href={BIKEREG_URL}
      class="inline-block bg-accent-green text-bg-base font-bold uppercase tracking-widest
             px-8 py-4 text-lg hover:opacity-90 transition-opacity mb-6"
    >
      Register Now
    </a>

    <p class="text-text-muted text-xs">
      $10 suggested donation to Great Lakes Recovery Centers
    </p>

    <p class="text-text-muted text-sm mt-6">
      Subject: <span class="redacted">mind control</span> gravel cycling
    </p>
  </div>
</section>
```

Key differences from current hero:
- Added `max-w-3xl` to the inner div for contained width
- Split the subtitle into two lines: date/location and distance/cost
- Added CountdownTimer component between subtitle and CTA
- Added "Register Now" CTA button with `bg-accent-green` background
- Added donation teaser line
- Distance shows "80 miles" per roadmap success criteria (not 100)
- Kept existing tone image, stamp, and redacted text

**Add second CTA between #route and #sectors.** Insert a new `<div>` immediately AFTER the
closing `</section>` of `#route` and BEFORE the opening `<section id="sectors">`:
```html
<div class="relative px-4 py-12 text-center border-t border-border">
  <p class="text-text-muted text-sm uppercase tracking-widest mb-4">
    Like what you see?
  </p>
  <a
    href={BIKEREG_URL}
    class="inline-block bg-accent-green text-bg-base font-bold uppercase tracking-widest
           px-8 py-4 text-lg hover:opacity-90 transition-opacity"
  >
    Register Now
  </a>
</div>
```

**Fill the #info section.** Replace the entire `<section id="info">` with:
```html
<section id="info" class="relative min-h-screen px-4 py-16 overflow-hidden border-t border-border">
  <img
    src="/tone/Mkultra-lsd-doc.jpg"
    alt=""
    class="tone-image inset-0 w-full h-full object-cover"
    loading="lazy"
  />
  <div class="relative z-10 max-w-2xl">
    <h2 class="text-3xl md:text-5xl mb-8">Event Info</h2>
    <EventInfoBlock />
  </div>
</section>
```

Key changes to #info:
- Removed `classified-border p-8` wrapper (the EventInfoBlock has its own structure)
- Added a tone image (Mkultra-lsd-doc.jpg) for visual consistency with other sections
- Added `max-w-2xl` to constrain content width
- Replaced Phase 7 placeholder with `<EventInfoBlock />`

**Summary of ALL changes to index.astro:**
1. Added imports: CountdownTimer, EventInfoBlock
2. Added BIKEREG_URL constant in frontmatter
3. Added `<link rel="preload" ... slot="head" />` before `<main>`
4. Rebuilt `<section id="hero">` with event details + countdown + CTA
5. Added second CTA `<div>` between #route and #sectors
6. Rebuilt `<section id="info">` with EventInfoBlock replacing placeholder

**IMPORTANT: Do NOT modify these sections:**
- `<section id="route">` — leave completely untouched
- `<section id="sectors">` — leave completely untouched
- `<section id="photos">` — leave completely untouched

IMPORTANT: The distance in the hero says "80 miles" per the roadmap success criteria. The
PROJECT.md says 80 miles. The route was extended but this number follows the official roadmap.
IMPORTANT: The BIKEREG_URL is a visible placeholder string. It will NOT work as a link. This is
intentional — the blocker in STATE.md notes it needs confirmation from the event director.
  </action>
  <verify>
Run `npx astro build` — must complete with zero errors. Then verify:

- Imports present:
  - `grep 'import CountdownTimer' src/pages/index.astro` returns match
  - `grep 'import EventInfoBlock' src/pages/index.astro` returns match
- Preload link:
  - `grep 'rel="preload"' src/pages/index.astro` returns match
  - `grep 'slot="head"' src/pages/index.astro` returns match
- Hero content in built HTML:
  - `grep 'Marquette Fire Bell' dist/index.html` returns match
  - `grep '80 miles' dist/index.html` returns match
  - `grep 'Register Now' dist/index.html` returns at least 2 matches (both CTAs)
- Countdown timer:
  - `grep 'data-target' dist/index.html` returns match
  - `grep 'cd-days' dist/index.html` returns match
- Info section:
  - `grep 'Great Lakes Recovery Centers' dist/index.html` returns match
  - `grep 'mk-ultra-gravel-2026.gpx' dist/index.html` returns match
  - `grep 'Download GPX' dist/index.html` returns match
- Placeholder removed:
  - `grep 'Phase 7' src/pages/index.astro` returns 0 matches
- Second CTA:
  - `grep 'Like what you see' dist/index.html` returns match
- Preload in head:
  - `grep 'preload.*CIA-MKULTRA' dist/index.html` returns match
  </verify>
  <done>
index.astro is fully updated: hero section has event details + countdown + CTA above fold,
second CTA sits between route and sectors sections, info section has EventInfoBlock with
donation info and GPX download, preload link is in the HTML head. Both CTA buttons use the
BIKEREG_URL placeholder. Build succeeds with all content in static HTML output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 7 content: rebuilt hero with event details + countdown timer + CTA, second CTA
below map, event info section with donation info and GPX download link.
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:4321 in a browser.

1. **Hero section (above fold):**
   - Verify "MK Ultra Gravel" title is prominent
   - Verify "June 7, 2026 -- Marquette, MI" is visible
   - Verify "Marquette Fire Bell -- 80 miles -- Free" is visible
   - Verify countdown timer shows days/hours/minutes and is ticking
   - Verify "Register Now" green CTA button is visible above fold
   - Verify "$10 suggested donation" teaser text is visible

2. **Second CTA (between route and sectors):**
   - Scroll past the map/elevation profile
   - Verify "Like what you see?" text and second "Register Now" button are visible
   - Verify it appears BEFORE the sectors section

3. **Event Info section (#info):**
   - Scroll to the Event Info section
   - Verify "The Format" block describes mass start, self-supported
   - Verify "The Cause" block names Great Lakes Recovery Centers and $10 donation
   - Verify "The Route File" block has a "Download GPX" button
   - Click "Download GPX" — verify it downloads a file named `mk-ultra-gravel-2026.gpx`

4. **Mobile (resize to ~375px width):**
   - Verify hero content is readable and CTA is visible without horizontal scroll
   - Verify countdown timer fits on mobile width

5. **Preload (DevTools Network tab):**
   - Check that `/tone/CIA-MKULTRA-IG_Page_01.jpg` loads with high priority (preload)

6. **CTA links:**
   - Both "Register Now" buttons should have href="PENDING — confirm with event director"
   - This is expected — the URL will be updated when confirmed
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks and checkpoint complete:

1. `grep 'CountdownTimer' src/pages/index.astro` — at least 2 matches (import + usage)
2. `grep 'EventInfoBlock' src/pages/index.astro` — at least 2 matches (import + usage)
3. `grep 'Register Now' dist/index.html` — at least 2 matches (both CTAs)
4. `grep 'data-target' dist/index.html` — 1 match (countdown timer)
5. `grep 'mk-ultra-gravel-2026.gpx' dist/index.html` — 1 match (GPX download)
6. `grep 'Great Lakes Recovery Centers' dist/index.html` — at least 1 match
7. `grep 'Marquette Fire Bell' dist/index.html` — at least 1 match
8. `grep 'Phase 7' src/pages/index.astro` — 0 matches (placeholder removed)
9. `grep 'preload' dist/index.html` — at least 1 match (hero image)
10. Build succeeds with no errors
</verification>

<success_criteria>
- Event date, start location, distance, and cost are visible in the hero (above fold)
- Two "Register Now" CTA buttons: one in hero, one below map section
- Countdown timer is live and ticking in the hero
- Event Info section has format description, GLRC donation info, and GPX download
- GPX download triggers with correct filename attribute
- Hero background image is preloaded via named head slot
- Both CTAs use BIKEREG_URL placeholder (acknowledged as pending)
- Human-verified: layout, content, and functionality confirmed on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/07-hero-event-info-ctas/07-03-SUMMARY.md`
</output>
