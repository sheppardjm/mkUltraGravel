---
phase: 23-new-photos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/photo-manifest.js
  - scripts/generate-data.js
  - src/components/PhotoGallery.astro
  - src/components/GravelSectors.astro
  - src/components/KomSegments.astro
autonomous: true

must_haves:
  truths:
    - "Down Jeep photo (68686675_*.jpg) appears as a map marker near mi 83.8 and displays in the gallery"
    - "Billie Helmer B&W photo (photo-1675213442182-*.avif) appears as a map marker near mi 22.1 and displays in the gallery"
    - "Gallery shows 55 photos total (up from 53)"
    - "Down Jeep sector card (mi 83.55-84.15) uses the new Down Jeep photo as its cover, not the fallback"
    - "Billie Helmer KOM card (mi 21.9-22.59) uses the new Billie Helmer photo as its cover, not the fallback"
  artifacts:
    - path: "public/data/photos.json"
      provides: "55 photo entries with lat/lng positions"
      contains: "68686675_2890293017652424_6952024628709556224_n.jpg"
    - path: "public/images/68686675_2890293017652424_6952024628709556224_n.jpg"
      provides: "Down Jeep photo served to browser"
    - path: "public/images/photo-1675213442182-24e1c1671387.avif"
      provides: "Billie Helmer B&W photo served to browser"
    - path: "public/images/thumbs/68686675_2890293017652424_6952024628709556224_n.webp"
      provides: "Down Jeep thumbnail for gallery"
    - path: "public/images/thumbs/photo-1675213442182-24e1c1671387.webp"
      provides: "Billie Helmer thumbnail for gallery"
  key_links:
    - from: "scripts/photo-manifest.js"
      to: "public/data/photos.json"
      via: "generate-data.js pipeline (match-photos.js)"
      pattern: "68686675.*mi.*83"
    - from: "scripts/generate-data.js"
      to: "public/images/photo-1675213442182-24e1c1671387.avif"
      via: "image copy filter must include avif extension"
      pattern: "avif"
    - from: "src/components/PhotoGallery.astro"
      to: "public/images/thumbs/*.webp"
      via: "filename.replace regex must handle .avif -> .webp"
      pattern: "avif"
---

<objective>
Integrate two new photos (Down Jeep + Billie Helmer B&W) into the pipeline so they appear on the map, in the gallery, and as card cover photos for their respective annotation ranges.

Purpose: Fixes the active blocker where Down Jeep KOM (mi 83-84) falls back to a distant photo, and adds Billie Helmer B&W coverage in the mi 21-23 range. Satisfies PHOTO-01 and PHOTO-02 requirements.
Output: Updated pipeline outputs (photos.json, annotations.json, thumbnails, card crops) with 55 photos.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-new-photos/23-RESEARCH.md
@scripts/photo-manifest.js
@scripts/generate-data.js
@src/components/PhotoGallery.astro
@src/components/GravelSectors.astro
@src/components/KomSegments.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AVIF support and new manifest entries</name>
  <files>
    scripts/generate-data.js
    scripts/photo-manifest.js
  </files>
  <action>
  1. In `scripts/generate-data.js` line 26, extend the image copy filter regex to include AVIF:
     Change: `/\.(jpg|jpeg|png|webp)$/i`
     To: `/\.(jpg|jpeg|png|webp|avif)$/i`

  2. In `scripts/photo-manifest.js`, add two new entries to the `photoManifest` array:

     After the last entry in the "Mi 66-81" section (AU6maRolPI2hBS7Tu7... at mi 80.2), add:
     ```javascript
     // -- Mi 83-84: Down Jeep sector --
     { filename: '68686675_2890293017652424_6952024628709556224_n.jpg', mi: 83.8 },
     ```

     After the first entry in the "Mi 19-25" section (ipimpW4SR0M8AGHKeltyECQ... at mi 19.6), insert:
     ```javascript
     // -- Mi 21-23: Billie Helmer KOM --
     { filename: 'photo-1675213442182-24e1c1671387.avif', mi: 22.1 },
     ```
     Note: Insert it between the mi 19.6 and mi 21.1 entries so the manifest stays in mile-order. Alternatively, place it between the mi 21.1 and mi 25.5 entries -- either location works as match-photos.js sorts by mile marker. The mi value of 22.1 is what matters (must be within the Billie Helmer KOM range of 21.9-22.59).

  3. Update the manifest header comment from "53 route photos" to "55 route photos".

  WHY these mile markers: mi 83.8 is the midpoint of the Down Jeep sector (83.55-84.15), ensuring assign-card-photos.js Pass 1 selects it. mi 22.1 is within the Billie Helmer KOM range (21.9-22.59), ensuring Pass 1 selects it instead of the fallback at mi 21.1.
  </action>
  <verify>
  Run: `grep -c "filename:" scripts/photo-manifest.js` -- expect 55
  Run: `grep "avif" scripts/generate-data.js` -- should show the updated regex with avif
  Run: `grep "83.8" scripts/photo-manifest.js` -- should show Down Jeep entry
  Run: `grep "22.1" scripts/photo-manifest.js` -- should show Billie Helmer entry
  </verify>
  <done>photo-manifest.js contains 55 entries including both new photos at correct mile markers; generate-data.js copies AVIF files</done>
</task>

<task type="auto">
  <name>Task 2: Fix AVIF extension handling in component templates and run pipeline</name>
  <files>
    src/components/PhotoGallery.astro
    src/components/GravelSectors.astro
    src/components/KomSegments.astro
  </files>
  <action>
  1. In `src/components/PhotoGallery.astro` line 24, extend the thumbnail path regex:
     Change: `.replace(/\.(jpg|jpeg|png)$/i, '.webp')`
     To: `.replace(/\.(jpg|jpeg|png|avif)$/i, '.webp')`

  2. In `src/components/GravelSectors.astro` line 30, extend the card path regex:
     Change: `.replace(/\.(jpg|jpeg|png)$/i, '.webp')`
     To: `.replace(/\.(jpg|jpeg|png|avif)$/i, '.webp')`

  3. In `src/components/KomSegments.astro` line 23, extend the card path regex:
     Change: `.replace(/\.(jpg|jpeg|png)$/i, '.webp')`
     To: `.replace(/\.(jpg|jpeg|png|avif)$/i, '.webp')`

  4. Run the full data pipeline:
     ```bash
     PATH=/usr/local/opt/node@25/bin/:$PATH node scripts/generate-data.js
     ```
     Expect output showing "Copied 55 images" (up from 53) and no errors or WARNING lines from assign-card-photos.js (both new photos should be selected by Pass 1, not fallback).

  5. Verify pipeline outputs:
     - `cat public/data/photos.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d))"` -- expect 55
     - `grep "68686675" public/data/photos.json` -- Down Jeep photo present with lat/lng
     - `grep "photo-1675213442182" public/data/photos.json` -- Billie Helmer photo present with lat/lng
     - `ls public/images/thumbs/68686675*.webp` -- thumbnail exists
     - `ls public/images/thumbs/photo-1675213442182*.webp` -- thumbnail exists
     - `ls public/images/cards/` -- card crops exist for both new photos (if selected as cover photos)
     - `grep "68686675" public/data/annotations.json` -- Down Jeep photo used as coverPhoto for the Down Jeep sector
     - `grep "photo-1675213442182" public/data/annotations.json` -- Billie Helmer photo used as coverPhoto for Billie Helmer KOM

  6. Build the site to verify no template errors:
     ```bash
     PATH=/usr/local/opt/node@25/bin/:$PATH npx astro build
     ```
     Expect clean build with no errors.
  </action>
  <verify>
  All pipeline checks from step 5 pass. Astro build completes without errors. No WARNING lines in pipeline output about fallback photo selection.
  </verify>
  <done>Pipeline produces 55 photos in photos.json; both new photos have thumbnails, are positioned on the route, and are assigned as cover photos for their respective cards; site builds cleanly</done>
</task>

</tasks>

<verification>
1. `cat public/data/photos.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d))"` returns 55
2. `grep -c "coverPhoto" public/data/annotations.json` shows all sectors and KOMs have cover photos
3. No "WARNING" or "fallback" messages in pipeline output for Down Jeep or Billie Helmer assignments
4. `ls public/images/thumbs/ | wc -l` shows 55 thumbnails
5. `ls public/images/cards/ | wc -l` shows card crops for all annotated segments
6. Astro build completes without errors
</verification>

<success_criteria>
- photos.json contains exactly 55 entries
- Down Jeep photo (68686675_*.jpg) mapped near mi 83.8 with lat/lng coordinates
- Billie Helmer B&W photo (photo-1675213442182-*.avif) mapped near mi 22.1 with lat/lng coordinates
- Down Jeep sector coverPhoto in annotations.json is 68686675_*.jpg (not fallback)
- Billie Helmer KOM coverPhoto in annotations.json is photo-1675213442182-*.avif (not fallback)
- WebP thumbnails exist for both new photos in public/images/thumbs/
- AVIF file copied to public/images/ (browser-servable)
- Site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-new-photos/23-01-SUMMARY.md`
</output>
