---
phase: 60-down-jeep-elevation-label-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ElevationProfile.astro]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Haavisto and Akkala Rd sector labels do not collide or overlap at any desktop viewport width (640px+), including below 1576px"
    - "All other sector labels remain visually unchanged — no new overlap introduced by increased offset"
  artifacts:
    - path: "src/components/ElevationProfile.astro"
      provides: "Sufficient vertical clearance between odd/even sector labels"
      contains: "yAdjust: i % 2 === 0 ? 0 : -28"
  key_links:
    - from: "yAdjust odd-index offset"
      to: "label vertical separation"
      via: "-28px clears the ~21.6px label height with 6.4px margin"
      pattern: "yAdjust.*-28"
---

<objective>
Fix the Haavisto/Akkala Rd sector label collision on the elevation profile at viewports under 1576px.

Purpose: UAT test 3 revealed that Haavisto (index 3, odd) and Akkala Rd (index 2, even) labels overlap below ~1576px because the generic yAdjust of -16px does not clear the ~21.6px label height. These are the closest adjacent sectors (3.49mi apart on a 110mi chart). Increasing the odd-index offset from -16 to -28 provides full vertical clearance at all desktop widths.
Output: Single line change in ElevationProfile.astro on line 83.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-down-jeep-elevation-label-fix/60-01-SUMMARY.md
@.planning/debug/haavisto-akkala-label-collision.md
@src/components/ElevationProfile.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase odd-index yAdjust from -16 to -28 for full label clearance</name>
  <files>src/components/ElevationProfile.astro</files>
  <action>
In `src/components/ElevationProfile.astro`, line 83, change the odd-index yAdjust value from -16 to -28.

Change:
```javascript
yAdjust: i % 2 === 0 ? 0 : -16,
```
To:
```javascript
yAdjust: i % 2 === 0 ? 0 : -28,
```

Why -28: Each sector label is 2 lines (name + stars) at 9px Space Mono with 1.2 line-height = ~21.6px tall. The offset must exceed 21.6px to prevent vertical overlap. -28px provides 6.4px of breathing room. This is Option B from the debug session — a general improvement that is robust against future sector additions.

Why this is safe for all sectors: The next-closest adjacent pair after Akkala Rd/Haavisto is Forest Service Rd/C4 at 7.6mi apart (~100px at 1576vw). At -28px offset, that pair still has comfortable horizontal clearance at all viewports. No other label pair is close enough to be affected.

CRITICAL constraints:
- Change ONLY the numeric value -16 to -28 on line 83. Touch nothing else.
- Do NOT add per-index overrides (no `i === 3` special-casing).
- Do NOT change any other label properties (position, xAdjust, rotation, font, color, display, content).
- Do NOT touch KOM annotation labels.
  </action>
  <verify>
1. Run `grep -n 'yAdjust:' src/components/ElevationProfile.astro` — should show line 83 with value `-28` (not -16).
2. Run `grep -c 'yAdjust: i % 2 === 0 ? 0 : -28' src/components/ElevationProfile.astro` — should return `1`.
3. Run `grep -c 'yAdjust: i % 2 === 0 ? 0 : -16' src/components/ElevationProfile.astro` — should return `0` (old value gone).
4. Run the project build command — should complete without errors.
  </verify>
  <done>
Line 83 of ElevationProfile.astro reads `yAdjust: i % 2 === 0 ? 0 : -28,`. Build passes. No other lines changed. Haavisto label now has 28px vertical clearance from Akkala Rd, exceeding the 21.6px label height.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'yAdjust: i % 2 === 0 ? 0 : -28' src/components/ElevationProfile.astro` returns `1`
2. `grep -c '? 0 : -16' src/components/ElevationProfile.astro` returns `0`
3. `grep -c 'rotation: 0' src/components/ElevationProfile.astro` returns `1` (unchanged from 60-01)
4. `grep -c "isNarrow ? 'start' : 'center'" src/components/ElevationProfile.astro` returns `1` (unchanged from 60-01)
5. Build completes without errors
</verification>

<success_criteria>
- Single value change on line 83: -16 becomes -28
- No other lines modified in ElevationProfile.astro
- No new files created, no new dependencies
- Build passes cleanly
- Haavisto and Akkala Rd labels have full vertical clearance at all desktop viewports (640px+)
</success_criteria>

<output>
After completion, create `.planning/phases/60-down-jeep-elevation-label-fix/60-02-SUMMARY.md`
</output>
