---
phase: 08-photo-gallery-lightbox
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 33 photos are visible in the gallery grid on desktop and mobile viewports"
    - "Clicking a thumbnail opens a full-screen lightbox showing the correct full-size image"
    - "The lightbox closes with Escape key, close button, and clicking outside the image"
    - "Gallery thumbnails load quickly (WebP, lazy-loaded) without visible delay"
    - "PhotoSwipe UI is dark-themed, matching the site design"
  artifacts: []
  key_links: []
---

<objective>
Verify the photo gallery and lightbox work correctly on desktop and mobile viewports.

Purpose: Human verification ensures the gallery grid looks right visually, thumbnails load quickly, the lightbox opens/closes correctly, and the dark theme is cohesive. Automated tests cannot catch visual layout issues or interaction feel.

Output: Verification pass/fail with any issues documented for gap closure.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-photo-gallery-lightbox/08-01-SUMMARY.md
@.planning/phases/08-photo-gallery-lightbox/08-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Photo gallery grid with 33 route photos displayed as WebP thumbnails in a responsive CSS grid, with PhotoSwipe 5 lightbox for full-screen viewing. Dark-themed to match the site's brutalist design.</what-built>
  <how-to-verify>
Start the dev server: `npm run dev`
Open http://localhost:4321 and scroll to the "Route Photos" section.

**Gallery Grid (5 checks):**
1. Count: All 33 photos are visible in the grid (may need to scroll within the section)
2. Layout: Grid shows 2 columns on mobile (~375px), 3 on tablet (~768px), 4 on desktop (~1024px+)
3. Thumbnails: Images are sharp and properly cropped (no stretching, no blank spaces in grid cells)
4. Load speed: Scroll to the photos section — thumbnails should appear quickly without visible loading delay
5. Theme: Grid fits the dark brutalist aesthetic — no bright/white borders or gaps that break the design

**Lightbox (5 checks):**
6. Open: Click any thumbnail — PhotoSwipe lightbox opens showing the full-size image
7. Navigation: Use arrow keys or swipe to move between photos
8. Close - button: Click the X button in the top-right corner — lightbox closes
9. Close - Escape: Open lightbox again, press Escape key — lightbox closes
10. Close - background: Open lightbox again, click outside the image (on the dark overlay) — lightbox closes
11. Theme: Lightbox background is dark (near-black), close/navigation icons are light — matches site palette
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 11 visual/interaction checks pass on human inspection:
- 5 gallery grid checks (count, layout, thumbnails, speed, theme)
- 6 lightbox checks (open, navigation, 3 close methods, theme)
</verification>

<success_criteria>
- Human confirms all 33 photos visible in grid
- Human confirms lightbox opens, navigates, and closes via all 3 methods
- Human confirms dark theme is cohesive
- No visual bugs or interaction failures reported
</success_criteria>

<output>
After completion, create `.planning/phases/08-photo-gallery-lightbox/08-03-SUMMARY.md`
</output>
