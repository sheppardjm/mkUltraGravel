---
phase: 41-gpx-route-update-pipeline-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/parse-gpx.js
  - public/data/route-data.json
  - public/data/annotations.json
  - public/data/photos.json
  - public/mk-ultra.gpx
  - MKULTRA.gpx
  - MK_Ultra.gpx
autonomous: true

must_haves:
  truths:
    - "route-data.json reports totalMi in the range 99-101 miles"
    - "npm run prebuild completes cleanly with zero warnings about clamping or mile-marker-exceeds-route-end"
    - "All 9 annotation mile markers (6 sectors + 3 KOMs) resolve without clamping warnings"
    - "photos.json photo positions are within route bounds — no mile marker exceeds route end"
    - "MKULTRA.gpx is tracked in git so Netlify builds succeed"
    - "MK_Ultra.gpx is removed from the repo"
  artifacts:
    - path: "scripts/parse-gpx.js"
      provides: "GPX source path pointing to MKULTRA.gpx"
      contains: "MKULTRA.gpx"
    - path: "public/data/route-data.json"
      provides: "Regenerated route data from new GPX"
      contains: "totalMi"
    - path: "public/data/annotations.json"
      provides: "Sector and KOM coordinates resolved against new route geometry"
    - path: "public/data/photos.json"
      provides: "Photo positions resolved against new route geometry"
    - path: "public/mk-ultra.gpx"
      provides: "Browser-downloadable copy of the new GPX"
  key_links:
    - from: "scripts/parse-gpx.js"
      to: "MKULTRA.gpx"
      via: "GPX_SOURCE constant on line 29"
      pattern: "MKULTRA\\.gpx"
    - from: "public/data/route-data.json"
      to: "scripts/parse-gpx.js"
      via: "pipeline output — parse-gpx writes route-data.json"
    - from: "public/data/annotations.json"
      to: "public/data/route-data.json"
      via: "resolve-annotations reads route-data.json to resolve mile markers"
---

<objective>
Replace the route GPX source from MK_Ultra.gpx to MKULTRA.gpx, regenerate all downstream pipeline artifacts, verify correctness, and clean up the old GPX file.

Purpose: The route has been updated on Strava. The new MKULTRA.gpx (2581 trackpoints, 100.62 mi) replaces the old MK_Ultra.gpx (2779 trackpoints, 100.71 mi). All downstream data — hero stats, elevation profile, map polyline, sector/KOM coordinates, and photo positions — must derive from the new file.
Output: Updated route-data.json, annotations.json, photos.json, public/mk-ultra.gpx, and cleaned-up repo with old GPX removed.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-gpx-route-update-pipeline-validation/41-RESEARCH.md
@scripts/parse-gpx.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch GPX source and regenerate pipeline</name>
  <files>scripts/parse-gpx.js</files>
  <action>
  1. In `scripts/parse-gpx.js` line 29, change:
     ```javascript
     const GPX_SOURCE = path.join(ROOT, 'MK_Ultra.gpx');
     ```
     to:
     ```javascript
     const GPX_SOURCE = path.join(ROOT, 'MKULTRA.gpx');
     ```
     This is the ONLY code change in the entire phase.

  2. Run `npm run data` (or `npm run prebuild`) from the repo root to regenerate all downstream artifacts.

  3. Capture the full pipeline stdout/stderr output for verification in Task 2.
  </action>
  <verify>
  - `npm run data` exits with code 0
  - No "ERROR" or "WARNING" lines in pipeline output
  - No "clamping" warnings from resolve-annotations.js
  - No "mile marker exceeds route end" warnings from match-photos.js
  </verify>
  <done>Pipeline completes cleanly on MKULTRA.gpx with zero errors or warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Verify regenerated artifacts and clean up old GPX</name>
  <files>
    public/data/route-data.json
    public/data/annotations.json
    public/data/photos.json
    public/mk-ultra.gpx
    MKULTRA.gpx
    MK_Ultra.gpx
  </files>
  <action>
  1. **Verify route-data.json:**
     - Read `public/data/route-data.json` and confirm `meta.totalMi` is in range 99-101 (expected: ~100.62).
     - Confirm `meta.trackpoints` equals 2581.
     - Confirm `meta.elevationGainFt` is present and numeric (expected: ~3365).

  2. **Verify annotations.json:**
     - Read `public/data/annotations.json` and confirm all sector and KOM entries have valid `lat`, `lon`, and `mi` fields.
     - Confirm no `mi` values exceed `meta.totalMi` from route-data.json.
     - Count: expect 6 sectors and 3 KOMs (9 total annotation mile markers).

  3. **Verify photos.json:**
     - Read `public/data/photos.json` and confirm all entries have `mi` values less than `meta.totalMi`.
     - Confirm the maximum `mi` value in photos.json is approximately 83.8 (well within route bounds).

  4. **Verify public/mk-ultra.gpx:**
     - Confirm `public/mk-ultra.gpx` exists and its file size is approximately the same as `MKULTRA.gpx` (it should be a copy).

  5. **Remove old GPX file:**
     - Run `git rm MK_Ultra.gpx` to remove the old GPX source from the repo.

  6. **Stage new files for commit:**
     - `git add MKULTRA.gpx` (currently untracked — must be tracked for Netlify builds).
     - `git add scripts/parse-gpx.js public/data/route-data.json public/data/annotations.json public/data/photos.json public/mk-ultra.gpx`

  NOTE: The elevation gain changing from 3595 to ~3365 ft is EXPECTED — Strava smooths trackpoints differently than RideWithGPS. Annotation coordinates shifting by 84-282 meters is EXPECTED — both files represent the same route but have slightly different start points.
  </action>
  <verify>
  - `node -e "const d = require('./public/data/route-data.json'); console.log(d.meta.totalMi >= 99 && d.meta.totalMi <= 101 ? 'PASS' : 'FAIL')"` outputs PASS
  - `node -e "const d = require('./public/data/route-data.json'); console.log(d.meta.trackpoints)"` outputs 2581
  - `node -e "const a = require('./public/data/annotations.json'); const max = Math.max(...a.sectors.map(s=>s.mi), ...a.kom.map(k=>k.mi)); console.log(max < 101 ? 'PASS' : 'FAIL')"` outputs PASS
  - `node -e "const p = require('./public/data/photos.json'); const max = Math.max(...p.map(x=>x.mi)); console.log(max < 101 ? 'PASS' : 'FAIL')"` outputs PASS
  - `ls -la public/mk-ultra.gpx` shows the file exists
  - `git status` shows MK_Ultra.gpx deleted, MKULTRA.gpx staged, and all public/data/ files staged
  </verify>
  <done>All downstream artifacts verified correct. MKULTRA.gpx tracked in git. MK_Ultra.gpx removed. All changes staged and ready for commit.</done>
</task>

</tasks>

<verification>
Run the following checks after both tasks complete:

1. **Route distance:** `node -e "console.log(require('./public/data/route-data.json').meta)"` — totalMi should be ~100.62, trackpoints should be 2581
2. **Annotations in bounds:** `node -e "const a=require('./public/data/annotations.json'); const r=require('./public/data/route-data.json'); const all=[...a.sectors,...a.kom].map(x=>x.mi); console.log('max mi:', Math.max(...all), 'route end:', r.meta.totalMi)"` — max mi should be less than totalMi
3. **Photos in bounds:** `node -e "const p=require('./public/data/photos.json'); console.log('max photo mi:', Math.max(...p.map(x=>x.mi)))"` — should be ~83.8
4. **GPX file state:** `git status` shows MK_Ultra.gpx deleted, MKULTRA.gpx added, pipeline outputs modified
5. **Build test:** `npm run build` completes without errors (confirms Astro can consume the new data)
</verification>

<success_criteria>
- route-data.json reports totalMi in range 99-101 miles (SUCCESS CRITERION 1)
- npm run prebuild completes cleanly end-to-end (SUCCESS CRITERION 5)
- No pipeline clamping warnings in output (SUCCESS CRITERION 3)
- No "mile marker exceeds route end" warnings (SUCCESS CRITERION 4)
- All changes staged for commit: MKULTRA.gpx added, MK_Ultra.gpx removed, parse-gpx.js updated, public/data/* regenerated, public/mk-ultra.gpx updated
</success_criteria>

<output>
After completion, create `.planning/phases/41-gpx-route-update-pipeline-validation/41-01-SUMMARY.md`
</output>
