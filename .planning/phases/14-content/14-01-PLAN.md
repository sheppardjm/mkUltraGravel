---
phase: 14-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MkUltraExplainer.astro
  - src/styles/global.css
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "A section titled 'Why MK Ultra?' appears on the page between the hero and the map section"
    - "The section contains at least two paragraphs of factual CIA MK-Ultra program history"
    - "At least one real FOIA document reference (1977 Senate Select Committee) is cited"
    - "Hovering a redacted-reveal span reveals the hidden text with a color transition"
    - "The section uses the classified-border and stamp design system classes"
    - "prefers-reduced-motion disables the reveal transition"
  artifacts:
    - path: "src/components/MkUltraExplainer.astro"
      provides: "MK Ultra explainer section component"
      min_lines: 30
    - path: "src/styles/global.css"
      provides: ".redacted-reveal CSS variant"
      contains: ".redacted-reveal"
    - path: "src/pages/index.astro"
      provides: "MkUltraExplainer imported and placed between hero and #route"
      contains: "MkUltraExplainer"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/MkUltraExplainer.astro"
      via: "Astro component import"
      pattern: "import MkUltraExplainer"
    - from: "src/components/MkUltraExplainer.astro"
      to: "src/styles/global.css"
      via: ".redacted-reveal CSS class"
      pattern: "redacted-reveal"
---

<objective>
Build the MkUltraExplainer.astro component with CIA program history, redaction-reveal styling, FOIA references, and insert it between the hero and map sections in index.astro.

Purpose: Fulfills CONT-01 — the site explains its name with a thematic, interactive section that ties the ride branding to the real CIA MK-Ultra program.
Output: New MkUltraExplainer.astro component, .redacted-reveal CSS variant in global.css, component imported and rendered in index.astro.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-content/14-RESEARCH.md
@src/pages/index.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .redacted-reveal CSS variant to global.css</name>
  <files>src/styles/global.css</files>
  <action>
Add the `.redacted-reveal` CSS variant inside `@layer components`, immediately AFTER the existing `.redacted` block (after line 87). This is an ADDITIVE change — do NOT modify the existing `.redacted` class.

Add these rules:

```css
.redacted-reveal {
  background-color: var(--color-accent-white);
  color: var(--color-accent-white);
  padding: 0 0.25em;
  cursor: pointer;
  transition: color 0.2s ease, background-color 0.2s ease;
}
.redacted-reveal:hover,
.redacted-reveal:focus-visible {
  color: var(--color-bg-base);
  background-color: var(--color-accent-green);
}
@media (prefers-reduced-motion: reduce) {
  .redacted-reveal { transition: none; }
}
```

Key differences from `.redacted`:
- NO `user-select: none` (reveal must be accessible)
- NO `text-decoration: line-through` (cleaner reveal effect)
- HAS `cursor: pointer` (signals interactivity)
- HAS `transition` for smooth color swap
- HAS `:focus-visible` for keyboard accessibility
- HAS `prefers-reduced-motion` media query
  </action>
  <verify>
Grep `src/styles/global.css` for `.redacted-reveal` — must find the class definition. Grep for `prefers-reduced-motion` — must find the media query. Confirm `.redacted` block is UNCHANGED (still has `user-select: none`).
  </verify>
  <done>
`.redacted-reveal` variant exists in global.css @layer components. Existing `.redacted` class is untouched. Hover reveals text, focus-visible works, prefers-reduced-motion disables transition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MkUltraExplainer.astro and insert in index.astro</name>
  <files>src/components/MkUltraExplainer.astro, src/pages/index.astro</files>
  <action>
**Part A: Create `src/components/MkUltraExplainer.astro`**

Build a purely static Astro component (no frontmatter logic needed). Follow the existing section pattern used by other sections in index.astro (relative positioning, px-4/py-16, border-t border-border, tone-image background, relative z-10 content wrapper).

Structure:
```astro
---
// No frontmatter required — purely static HTML
---

<section id="explainer" class="relative px-4 py-16 overflow-hidden border-t border-border">
  <img
    src="/tone/MK-Ultra.webp"
    alt=""
    class="tone-image inset-0 w-full h-full object-cover"
    loading="lazy"
  />
  <div class="relative z-10 max-w-2xl">
    <p class="stamp mb-6">Declassified</p>
    <h2 class="text-3xl md:text-5xl mb-8">Why MK Ultra?</h2>
    <div class="classified-border p-6 md:p-8 space-y-6 text-text-body text-sm leading-relaxed">
```

Content must include:
1. **Paragraph 1:** Introduce MK-Ultra — the CIA ran Project <span class="redacted-reveal">MKULTRA</span> from 1953 to 1973. It was a covert program of illegal experiments on unwitting human subjects — testing LSD, hypnosis, sensory deprivation, and psychological torture. Subjects included mental patients, prisoners, drug addicts, and ordinary citizens who never consented.

2. **Paragraph 2:** Scale and institutional reach — the program funded research at 44 colleges and universities, 15 research foundations, 12 hospitals, and 3 prisons. CIA Director Richard Helms ordered the destruction of all MK-Ultra files in 1973. A cache of 20,000 documents survived because they had been misfiled in a financial records building, and were discovered during a 1977 Freedom of Information Act request.

3. **Paragraph 3:** Connection to the ride — This ride borrows the name as a nod to the Upper Peninsula's own history of isolation and endurance. 100 miles of gravel through remote forest roads will test your <span class="redacted-reveal">mental fortitude</span>. No mind control required — just legs and grit.

4. **FOIA citation:** At the bottom of the classified-border div, add a muted source line:
```html
<p class="text-text-muted text-xs border-t border-border pt-4 mt-2">
  Source: U.S. Senate Select Committee on Intelligence, 1977 &mdash;
  <em>Project MKULTRA, the CIA's Program of Research in Behavioral Modification</em>
  &mdash; FOIA-released documents (CIA MKULTRA Collection, National Security Archive)
</p>
```

Use `redacted-reveal` class (NOT `redacted`) on the words "MKULTRA" and "mental fortitude" as shown above. These are the interactive hover-reveal elements.

**Part B: Import and insert in `src/pages/index.astro`**

1. Add import at the top of the frontmatter (after existing component imports, before the BIKEREG_URL constant):
```
import MkUltraExplainer from "../components/MkUltraExplainer.astro";
```

2. Insert `<MkUltraExplainer />` in the template between the `</section>` closing tag of `#hero` (line 64) and the `<section id="route">` opening tag (line 66). This places the explainer "between event info and the map" — the hero provides the event introduction and the #route section has the map.

The inserted line should be:
```html
    <MkUltraExplainer />
```

Do NOT reorder any other sections. The explainer simply slots in between hero and route.
  </action>
  <verify>
1. Run `npm run build` — must succeed with no errors.
2. Grep `src/pages/index.astro` for `MkUltraExplainer` — must find import and usage.
3. Grep `src/components/MkUltraExplainer.astro` for `redacted-reveal` — must find at least 2 instances.
4. Grep `src/components/MkUltraExplainer.astro` for `Senate Select Committee` — must find the FOIA citation.
5. Confirm section order in index.astro: hero -> explainer -> route -> CTA -> sectors -> photos -> info.
  </verify>
  <done>
MkUltraExplainer.astro exists with CIA history content, two redacted-reveal interactive spans, FOIA citation, and classified-border/stamp styling. It is imported and rendered in index.astro between the hero and route sections. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without error
2. The built HTML contains a `<section id="explainer">` with "Why MK Ultra?" heading
3. The section contains at least two `redacted-reveal` spans
4. The section contains a FOIA citation referencing the 1977 Senate Select Committee
5. `.redacted` class in global.css is unchanged (still has `user-select: none`)
6. `.redacted-reveal` class exists in global.css with hover/focus-visible states and prefers-reduced-motion
7. Section order in index.astro: hero -> explainer -> route -> CTA -> sectors -> photos -> info
</verification>

<success_criteria>
- CONT-01 fully satisfied: MK Ultra name explainer section with CIA program history, redaction-reveal styling, and real FOIA document reference
- Build succeeds with no regressions
- Existing `.redacted` styling in hero unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-content/14-01-SUMMARY.md`
</output>
