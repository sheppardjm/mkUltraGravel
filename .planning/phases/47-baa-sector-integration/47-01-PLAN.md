---
phase: 47-baa-sector-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/resolve-annotations.js
  - data.md
  - src/lib/scoring.js
  - src/lib/scoring.test.js
  - src/pages/results.astro
  - src/components/GrinduroExplainer.astro
  - src/components/ScoringExplainer.astro
autonomous: true

must_haves:
  truths:
    - "BAA sector appears on the interactive map as a colored polyline at mile 12.9 with 2-star color"
    - "BAA sector appears as a labeled band on the elevation profile with 2-star color, name, and star rating"
    - "BAA sector card renders with pipeline-assigned cover photo, 2-star rating, and Strava link"
    - "Gravel Champion scoring engine counts 7 required sectors and BAA segment effort contributes to cumulative time"
    - "All existing tests pass with updated 7-sector expectations"
    - "Site builds successfully with BAA visible across all surfaces"
  artifacts:
    - path: "scripts/resolve-annotations.js"
      provides: "BAA sector definition in pipeline source of truth"
      contains: "BAA"
    - path: "src/lib/scoring.js"
      provides: "BAA segment ID in scoring engine"
      contains: "41159670"
    - path: "src/lib/scoring.test.js"
      provides: "Updated test fixtures for 7 sectors"
      contains: "41159670"
    - path: "src/pages/results.astro"
      provides: "BAA in SECTOR_NAMES map and /7 sectors display"
      contains: "41159670"
    - path: "public/data/annotations.json"
      provides: "Generated annotation data including BAA sector"
      contains: "BAA"
  key_links:
    - from: "scripts/resolve-annotations.js"
      to: "public/data/annotations.json"
      via: "npm run data pipeline"
      pattern: "BAA.*startMi.*12\\.9"
    - from: "src/lib/scoring.js"
      to: "src/pages/results.astro"
      via: "SECTOR_SEGMENT_IDS import"
      pattern: "41159670"
---

<objective>
Add BAA gravel sector (Strava segment 41159670, 2-star, mile 12.9, 2.53mi length) to the data pipeline, scoring engine, results page, and content components — making it the 7th gravel sector, fully integrated and indistinguishable from the existing 6.

Purpose: Complete v9.0 milestone by adding the BAA sector to every surface where sectors appear.
Output: BAA sector visible on map, elevation profile, sector card; scoring engine updated to 7 sectors; all tests green; site builds.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/resolve-annotations.js
@src/lib/scoring.js
@src/lib/scoring.test.js
@src/pages/results.astro
@src/components/GrinduroExplainer.astro
@src/components/ScoringExplainer.astro
@data.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BAA to data pipeline and content components</name>
  <files>
    scripts/resolve-annotations.js
    data.md
    src/components/GrinduroExplainer.astro
    src/components/ScoringExplainer.astro
  </files>
  <action>
  1. In `scripts/resolve-annotations.js`, insert BAA as the FIRST entry in the `sectors` array (position 0, since mile 12.9 is before all existing sectors starting at 23.4):

     ```javascript
     { name: "BAA",               startMi: 12.9,  lengthMi: 2.53, stars: 2, stravaSegmentId: 41159670 },
     ```

     Insert this line immediately before the existing Sandstrom entry. Keep alignment consistent with the other entries.

  2. In `data.md`, add BAA as entry 1 in the "Gravel sectors" list (before Sandstrom) and renumber all subsequent entries:

     ```
     1. BAA, 12.9mi into route, 2.53mi, 2-star
     2. Sandstrom, 23.4mi into route, 5.89mi, 3-star
     ...
     7. Down Jeep, 83.55mi into route, .6mi, 5-star
     ```

  3. In `src/components/GrinduroExplainer.astro`, change "Six gravel sectors" to "Seven gravel sectors".

  4. In `src/components/ScoringExplainer.astro`, change "all 6 timed sectors" to "all 7 timed sectors".
  </action>
  <verify>
  Run `node scripts/resolve-annotations.js` and confirm:
  - Output shows 7 resolved sectors (not 6)
  - BAA appears first with start coordinates and track points
  - No warnings or errors

  Inspect the generated `public/data/annotations.json` and confirm BAA entry exists with name "BAA", startMi near 12.9, stars 2, stravaSegmentId 41159670, and non-empty track array.
  </verify>
  <done>
  - BAA is the first sector in resolve-annotations.js (mile 12.9, before Sandstrom at 23.4)
  - data.md lists 7 gravel sectors with BAA first
  - GrinduroExplainer says "Seven gravel sectors"
  - ScoringExplainer says "all 7 timed sectors"
  - annotations.json contains BAA with resolved coordinates and track
  </done>
</task>

<task type="auto">
  <name>Task 2: Add BAA to scoring engine, tests, and results page</name>
  <files>
    src/lib/scoring.js
    src/lib/scoring.test.js
    src/pages/results.astro
  </files>
  <action>
  1. In `src/lib/scoring.js`:
     - Add `"41159670", // BAA` to `SECTOR_SEGMENT_IDS` array (insert as first entry to match pipeline order, or append — order does not affect scoring logic, but consistency with pipeline order is preferred: add as first entry)
     - Update JSDoc comment from "The 6 gravel sector segment IDs" to "The 7 gravel sector segment IDs"
     - Update `computeGravelChampion` JSDoc comment from "finished all 6 sectors" to "finished all 7 sectors"

  2. In `src/lib/scoring.test.js`:
     - Add `"41159670"` as the first entry in the `SECTOR_IDS` fixture array
     - Update `makeGravelAthlete`: change `Math.floor(totalTime / 6)` to `Math.floor(totalTime / 7)` and `totalTime - perSector * 5` to `totalTime - perSector * 6`, and update the `times` array to have 7 entries (add one more `perSector` element): `[perSector, perSector, perSector, perSector, perSector, perSector, remainder]`
     - Update JSDoc comment from "all 6 sector times" to "all 7 sector times"
     - Update `expect(first.completedSectors).toBe(6)` to `.toBe(7)` in test 1
     - Update `expect(SECTOR_SEGMENT_IDS).toHaveLength(6)` to `.toHaveLength(7)` in the constants sanity check
     - Add `expect(SECTOR_SEGMENT_IDS).toContain("41159670");` to the "contains expected IDs" test
     - In test 3 (DNF test), the comment says "5 of 6 sectors" — update to "6 of 7 sectors". The test deletes segment "6809754" (Down Jeep), so `completedSectors` will be 6 (not 5) since there are now 7 total. Update `expect(result.M[2].completedSectors).toBe(5)` to `.toBe(6)`.

  3. In `src/pages/results.astro`:
     - Add `"41159670": "BAA",` as the first entry in the `SECTOR_NAMES` map (around line 22)
     - Change all three occurrences of `/6 sectors]` to `/7 sectors]` (lines ~176, ~240, ~304 — the DNF display spans for M, F, NB tabs)

  4. Run the full verification pipeline:
     ```bash
     npm run data          # regenerate annotations.json with BAA + assign cover photo
     npm test              # verify all tests pass with 7-sector scoring
     npm run build         # verify full site builds successfully
     ```
  </action>
  <verify>
  - `npm test` passes with all existing tests updated for 7 sectors
  - `npm run build` succeeds without errors
  - In the built output, grep for "BAA" in the generated HTML to confirm sector card and results page include it
  </verify>
  <done>
  - scoring.js SECTOR_SEGMENT_IDS contains "41159670" and has 7 entries
  - scoring.test.js passes with 7-sector fixtures, makeGravelAthlete produces 7 sector times, completedSectors expectations updated
  - results.astro SECTOR_NAMES includes BAA, DNF display shows /7 sectors
  - `npm test` green, `npm run build` green
  </done>
</task>

</tasks>

<verification>
1. `npm test` — all scoring tests pass with 7-sector expectations
2. `npm run data` — annotations.json has 7 sectors, BAA first with resolved coordinates
3. `npm run build` — site builds without errors
4. Inspect `public/data/annotations.json` — BAA sector has name, startMi, lengthMi, stars, stravaSegmentId, lat, lon, endLat, endLon, track array
5. Grep built HTML for "BAA" — appears in sector card section
6. Grep built HTML for "/7 sectors" — appears in results page DNF labels
7. Grep built HTML for "Seven gravel sectors" — appears in GrinduroExplainer
8. Grep built HTML for "all 7 timed sectors" — appears in ScoringExplainer
</verification>

<success_criteria>
- BAA is the 7th gravel sector, visible on map, elevation profile, and sector cards (driven by annotations.json)
- Scoring engine counts 7 required sectors; BAA segment ID "41159670" is in SECTOR_SEGMENT_IDS
- All tests pass; site builds; no regressions in existing 6 sectors
</success_criteria>

<output>
After completion, create `.planning/phases/47-baa-sector-integration/47-01-SUMMARY.md`
</output>
