---
phase: 21-escher-background-favicon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/favicon.svg]
autonomous: true

must_haves:
  truths:
    - "Browser tab displays a Penrose triangle icon instead of the 'MK' text placeholder"
    - "Triangle uses three tonal green fills that communicate a 3D impossible-triangle illusion at 32px"
    - "Favicon renders correctly on the dark background visible in browser tabs"
  artifacts:
    - path: "public/favicon.svg"
      provides: "Penrose triangle favicon"
      contains: "polygon"
  key_links:
    - from: "src/layouts/BaseLayout.astro:23"
      to: "public/favicon.svg"
      via: "link rel=icon href=/favicon.svg"
      pattern: 'href="/favicon.svg"'
---

<objective>
Replace the "MK" text placeholder favicon with a Penrose triangle SVG using the site's accent green palette.

Purpose: The Penrose impossible triangle is the visual identity mark for MK Ultra Gravel — it replaces a generic text placeholder with a distinctive geometric icon that reads clearly at 16-32px in browser tabs.
Output: Updated `public/favicon.svg` containing three polygon elements forming a Penrose triangle.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-escher-background-favicon/21-RESEARCH.md
@public/favicon.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace favicon.svg with Penrose triangle polygons</name>
  <files>public/favicon.svg</files>
  <action>
Replace the entire content of `public/favicon.svg` with a Penrose triangle SVG.

Use a 32x32 viewBox. The SVG contains:

1. A background `<rect>` filled with the dark navy background hex `#14141e` (hex equivalent of `oklch(0.10 0.01 250)` — use hex, not oklch, because oklch in SVG fill attributes has inconsistent browser support).

2. A `<g>` wrapper with `transform="scale(0.114, 0.132)"` containing three `<path>` elements adapted from the Wikipedia Penrose triangle SVG (public domain, 280x243 viewport):

   - Face 1 (brightest): `d="M 55.0625,182.4375 L 80.6875,182.5625 L 151.21875,59.84375 L 252.21615,228.15076 L 264.76729,205.25818 L 151.5625,13.25 Z"` fill `#a3f0a0`
   - Face 2 (mid tone): `d="M 15.625,206.0625 L 27.5,228.4375 L 252.30454,228.28334 L 151.23246,59.859344 L 138.54873,81.934334 L 211.73429,205.92661 Z"` fill `#6db86a`
   - Face 3 (darkest): `d="M 124.05609,12.990601 L 15.638759,206.0253 L 211.76418,205.96833 L 197.88064,182.51185 L 54.948939,182.532 L 151.67357,13.253813 Z"` fill `#3d7a3a`

These hex values are the equivalents of the oklch accent green tones from the research:
- `#a3f0a0` = oklch(0.85 0.24 145) — matches `--color-accent-green`
- `#6db86a` = oklch(0.65 0.20 145)
- `#3d7a3a` = oklch(0.45 0.16 145)

No changes to BaseLayout.astro — the existing `<link rel="icon" href="/favicon.svg" type="image/svg+xml" />` at line 23 already points to this file.
  </action>
  <verify>
1. Run `cat public/favicon.svg` and confirm it contains three `path` elements with the Penrose triangle coordinates and hex fills.
2. Run `npx astro build` and confirm the build succeeds with no errors.
3. Confirm the file is valid SVG by checking it starts with `<svg xmlns=` and contains a proper viewBox.
  </verify>
  <done>
`public/favicon.svg` contains a Penrose triangle with three green-toned faces on a dark background, renderable at 32x32. The existing favicon link tag in BaseLayout.astro requires no changes. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `public/favicon.svg` exists and contains `polygon` or `path` elements (not `<text>`)
2. The SVG uses hex color fills (not oklch) for maximum browser compatibility
3. `npx astro build` completes without errors
4. The file has a 32x32 viewBox
</verification>

<success_criteria>
The browser tab favicon displays a Penrose triangle SVG instead of the "MK" text placeholder. The triangle uses three tonal green fills on a dark background. No build errors.
</success_criteria>

<output>
After completion, create `.planning/phases/21-escher-background-favicon/21-01-SUMMARY.md`
</output>
