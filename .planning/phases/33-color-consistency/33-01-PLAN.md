---
phase: 33-color-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/starColors.ts
  - src/components/GravelSectors.astro
  - src/components/RouteMap.astro
  - src/components/ElevationProfile.astro
autonomous: true

must_haves:
  truths:
    - "A single shared module is the only place star-rating hex values are defined"
    - "Map polylines, elevation bands, and sector cards all render identical colors for every star rating"
    - "No inline starColors literal objects remain in RouteMap.astro, ElevationProfile.astro, or GravelSectors.astro"
  artifacts:
    - path: "src/lib/starColors.ts"
      provides: "Single source of truth for star-rating color map"
      exports: ["starColors"]
    - path: "src/components/GravelSectors.astro"
      provides: "Sector cards importing starColors from shared module"
      contains: "import { starColors } from"
    - path: "src/components/RouteMap.astro"
      provides: "Map polylines importing starColors from shared module"
      contains: "import { starColors } from"
    - path: "src/components/ElevationProfile.astro"
      provides: "Elevation bands importing starColors from shared module"
      contains: "import { starColors } from"
  key_links:
    - from: "src/components/GravelSectors.astro"
      to: "src/lib/starColors.ts"
      via: "frontmatter import"
      pattern: "import.*starColors.*from.*lib/starColors"
    - from: "src/components/RouteMap.astro"
      to: "src/lib/starColors.ts"
      via: "script tag import"
      pattern: "import.*starColors.*from.*lib/starColors"
    - from: "src/components/ElevationProfile.astro"
      to: "src/lib/starColors.ts"
      via: "script tag import"
      pattern: "import.*starColors.*from.*lib/starColors"
---

<objective>
Extract the `starColors` constant into a single shared module and update all three consumers to import from it, eliminating duplicate definitions and ensuring color consistency across map, elevation chart, and sector cards.

Purpose: Prevent color drift between the three components that render star-rating colors — currently three identical but independent definitions that could diverge.
Output: `src/lib/starColors.ts` shared module + updated imports in GravelSectors.astro, RouteMap.astro, ElevationProfile.astro
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/scoring.js (precedent for shared ES module used in both SSR and browser contexts)
@src/components/GravelSectors.astro
@src/components/RouteMap.astro
@src/components/ElevationProfile.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create starColors shared module</name>
  <files>src/lib/starColors.ts</files>
  <action>
Create `src/lib/starColors.ts` with the following content:

```typescript
/**
 * starColors.ts — Star-rating color palette
 *
 * Single source of truth for the Paris-Roubaix-style sector rating colors.
 * Used by: GravelSectors (SSR frontmatter), RouteMap (browser script), ElevationProfile (browser script).
 */
export const starColors: Record<number, string> = {
  1: '#f0c040',   // yellow — light gravel
  2: '#e8962a',   // gold-orange — moderate
  3: '#d9641e',   // burnt orange — getting rough
  4: '#c93a18',   // red-orange — hard
  5: '#b71c1c'    // deep red — brutal
};
```

This follows the same pattern as `src/lib/scoring.js` — a plain ES module with named exports, usable in both Node/build-time and browser contexts.
  </action>
  <verify>Confirm `src/lib/starColors.ts` exists and exports `starColors`.</verify>
  <done>File exists with the 5-color Record&lt;number, string&gt; export.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline starColors in all three consumers</name>
  <files>src/components/GravelSectors.astro, src/components/RouteMap.astro, src/components/ElevationProfile.astro</files>
  <action>
**GravelSectors.astro** (frontmatter import, SSR context):
- Remove the inline `const starColors: Record<number, string> = { ... };` block (lines 16-22 in frontmatter)
- Add `import { starColors } from '../lib/starColors';` to the frontmatter block (after the existing Props import/destructuring, before the closing `---`)
- All template usages of `starColors[sector.stars]` remain unchanged

**RouteMap.astro** (browser `<script>` tag):
- Remove the inline `const starColors: Record<number, string> = { ... };` block (lines 128-135 inside the `<script>` tag, including the comment on line 128)
- Add `import { starColors } from '../lib/starColors';` near the top of the `<script>` tag (after any existing imports, before the `document.addEventListener` or first executable code)
- All usages of `starColors[sector.stars]` throughout the script remain unchanged

**ElevationProfile.astro** (browser `<script>` tag):
- Remove the inline `const starColors: Record<number, string> = { ... };` block (lines 58-65 inside the `initElevation()` function, including the comment on line 58)
- Add `import { starColors } from '../lib/starColors';` near the top of the `<script>` tag (outside `initElevation()`, alongside any other top-level imports)
- All usages of `starColors[sector.stars]` within `initElevation()` remain unchanged — the variable is now module-scoped rather than function-scoped, which is fine since it's a constant
  </action>
  <verify>
1. Run `grep -rn "const starColors" src/components/` — should return zero matches (no inline definitions remain)
2. Run `grep -rn "import.*starColors.*from" src/components/` — should return exactly 3 matches (one per consumer)
3. Run the Astro build: `PATH="/usr/local/opt/node@25/bin:$PATH" npx astro build` — must succeed with no errors
  </verify>
  <done>
- Zero inline `starColors` definitions in any component file
- Three import statements pointing to `../lib/starColors`
- Astro build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "const starColors" src/` returns exactly ONE match — in `src/lib/starColors.ts`
2. `grep -rn "import.*starColors" src/components/` returns exactly THREE matches
3. `PATH="/usr/local/opt/node@25/bin:$PATH" npx astro build` succeeds
4. Visual spot-check: The built HTML/JS for sector cards, map polylines, and elevation bands all reference the same hex values (#f0c040, #e8962a, #d9641e, #c93a18, #b71c1c)
</verification>

<success_criteria>
- src/lib/starColors.ts is the single source of truth for star-rating colors
- No inline starColors objects remain in any component
- Astro build succeeds — the module works in both SSR (GravelSectors frontmatter) and browser (RouteMap/ElevationProfile script) contexts
- All five hex values are unchanged from their current values
</success_criteria>

<output>
After completion, create `.planning/phases/33-color-consistency/33-01-SUMMARY.md`
</output>
