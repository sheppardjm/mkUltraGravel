---
phase: 02-scaffold-design-system
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/styles/global.css
  - src/pages/index.astro
  - src/layouts/BaseLayout.astro
autonomous: false

must_haves:
  truths:
    - "A film-grain noise texture is visible over the entire page as a subtle overlay"
    - "At least two tone reference images (Escher/CIA/psychedelic) are visible on the base page"
    - "Redacted-document or surveillance visual motifs are present as design elements"
    - "The overall page feel is dark, brutalist, and psychedelic — not a friendly cycling website"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Grain overlay CSS, visual motif utility classes"
      contains: "grain"
    - path: "src/pages/index.astro"
      provides: "Tone images and surrealist visual elements integrated into sections"
      contains: "tone"
  key_links:
    - from: "src/pages/index.astro"
      to: "images/tone/"
      via: "Astro Image import or img src references"
      pattern: "tone/"
    - from: "src/styles/global.css"
      to: "src/pages/index.astro"
      via: "grain-overlay class applied to body or layout"
      pattern: "grain"
---

<objective>
Add the Escher/CIA/surrealist visual identity that makes this site look like a
declassified MK-Ultra document, not a generic cycling event page.

Purpose: This is the design differentiator. Without the visual motifs, the site is
just "dark background with monospaced text." The grain overlay, tone reference images,
and surrealist elements are what make it psychedelic and unsettling.

Output: Base page with grain texture overlay, integrated tone reference images,
and visual motifs (redacted text, distorted geometry, surveillance aesthetics).
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scaffold-design-system/02-RESEARCH.md
@.planning/phases/02-scaffold-design-system/02-02-SUMMARY.md
@src/styles/global.css
@src/layouts/BaseLayout.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add grain overlay and visual motif CSS utilities</name>
  <files>src/styles/global.css, src/layouts/BaseLayout.astro</files>
  <action>
  1. Add grain overlay to `src/styles/global.css` inside the `@layer components` block:

     ```css
     @layer components {
       .grain-overlay {
         position: fixed;
         inset: 0;
         pointer-events: none;
         opacity: 0.06;
         background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
         background-repeat: repeat;
         background-size: 200px 200px;
         z-index: 9999;
       }

       .redacted {
         background-color: var(--color-accent-white);
         color: var(--color-accent-white);
         padding: 0 0.25em;
         text-decoration: line-through;
         text-decoration-color: var(--color-bg-base);
         text-decoration-thickness: 0.15em;
         user-select: none;
       }

       .stamp {
         display: inline-block;
         border: 3px solid var(--color-accent-red);
         color: var(--color-accent-red);
         padding: 0.15em 0.5em;
         font-family: var(--font-display);
         text-transform: uppercase;
         letter-spacing: 0.15em;
         transform: rotate(-3deg);
         opacity: 0.85;
       }

       .classified-border {
         border: 1px solid var(--color-border);
         position: relative;
       }
       .classified-border::before {
         content: "CLASSIFIED";
         position: absolute;
         top: -0.7em;
         left: 1em;
         background-color: var(--color-bg-base);
         color: var(--color-accent-red);
         font-family: var(--font-display);
         font-size: 0.7em;
         letter-spacing: 0.2em;
         padding: 0 0.5em;
       }

       .tone-image {
         opacity: 0.12;
         mix-blend-mode: lighten;
         filter: grayscale(100%) contrast(1.3);
         position: absolute;
         pointer-events: none;
       }
     }
     ```

  2. Add the grain overlay div to `src/layouts/BaseLayout.astro` — insert it as the
     first child of `<body>`, before `<slot />`:

     ```html
     <body>
       <div class="grain-overlay" aria-hidden="true"></div>
       <slot />
     </body>
     ```

     The `aria-hidden="true"` ensures screen readers skip the decorative overlay.
  </action>
  <verify>
  Run `npx astro dev`, open localhost. Verify:
  - Subtle film-grain noise is visible over the entire page (look closely at solid-color areas)
  - Grain does not interfere with clicking — `pointer-events: none` must work
  - Page still scrolls normally
  Kill dev server.
  </verify>
  <done>Grain overlay covers entire viewport, redacted/stamp/classified-border/tone-image CSS utility classes exist in global.css</done>
</task>

<task type="auto">
  <name>Task 2: Integrate tone reference images and surrealist design elements</name>
  <files>src/pages/index.astro</files>
  <action>
  1. First, copy selected tone reference images into `public/tone/` so they're served
     as static assets. Pick 4-5 of the most impactful images from `images/tone/`:
     - `CIA-MKULTRA-IG_Page_01.jpg` (declassified document — perfect for background)
     - `escharian_stairs_fb.webp` (Escher stairs — geometric impossible architecture)
     - `MK-Ultra.webp` (MK-Ultra branding)
     - `Mkultra-lsd-doc.jpg` (another document aesthetic)
     - `lsd-mind-control.jpg` (psychedelic imagery)

     ```bash
     mkdir -p public/tone
     cp images/tone/CIA-MKULTRA-IG_Page_01.jpg public/tone/
     cp images/tone/escharian_stairs_fb.webp public/tone/
     cp images/tone/MK-Ultra.webp public/tone/
     cp images/tone/Mkultra-lsd-doc.jpg public/tone/
     cp images/tone/lsd-mind-control.jpg public/tone/
     ```

  2. Update `src/pages/index.astro` to integrate visual motifs into sections.
     The tone images should be atmospheric — ghosted backgrounds, not focal content.
     Use the `.tone-image` class (opacity 0.12, grayscale, lighten blend) for the images.

     Update the hero section to include:
     - A tone image positioned absolutely behind the hero text
     - A "CLASSIFIED" stamp element
     - Redacted text elements as atmospheric decoration

     Update the structure of each section to have `position: relative` and `overflow: hidden`
     so tone images can be positioned within them without breaking layout.

     Example hero integration:
     ```astro
     <section id="hero" class="relative min-h-screen flex items-center justify-center px-4 overflow-hidden">
       <img
         src="/tone/CIA-MKULTRA-IG_Page_01.jpg"
         alt=""
         class="tone-image inset-0 w-full h-full object-cover"
         loading="eager"
       />
       <div class="relative z-10 text-center">
         <p class="stamp mb-6">Classification: Ultra</p>
         <h1 class="text-5xl md:text-8xl tracking-tight mb-4">MK Ultra Gravel</h1>
         <p class="text-text-muted text-lg md:text-xl">
           June 7, 2026 &mdash; Marquette, MI &mdash; 100 miles
         </p>
         <p class="text-text-muted text-sm mt-4">
           Subject: <span class="redacted">mind control</span> gravel cycling
         </p>
       </div>
     </section>
     ```

     Add at least one more tone image to another section (e.g., Escher stairs behind
     the route section, psychedelic imagery behind photos section).

     Keep all placeholder section content from Plan 02-02 — just enhance with visual
     elements. Do NOT remove section IDs or headings.

  3. Add a subtle separator between sections — a thin accent-green horizontal rule
     or a `border-top: 1px solid var(--color-border)` on each section after hero.
  </action>
  <verify>
  Run `npx astro dev`, open localhost. Verify:
  - Hero section has ghosted CIA document image behind text
  - "Classification: Ultra" stamp is visible in red with slight rotation
  - "mind control" text appears redacted (blacked out)
  - At least one other section has a ghosted tone image
  - All 5 section IDs still present and scrollable
  - Tone images are atmospheric (low opacity, grayscale) not focal
  - Page load is not noticeably slow (images are reasonable size)
  Kill dev server.
  </verify>
  <done>4-5 tone images in public/tone/, hero section has CIA document background + stamp + redacted text, at least 2 sections have tone image backgrounds, all section structure preserved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete Phase 2 design system: dark brutalist psychedelic base page with:
  - Near-black background with film-grain noise overlay
  - Space Mono body text, Special Elite headings
  - CIA document / Escher / psychedelic tone images as ghosted backgrounds
  - "CLASSIFIED" stamps, redacted text blocks, surveillance aesthetic
  - Five scrollable sections (hero, route, sectors, photos, info)
  - Acid-green accents, blood-red stamps, off-white headings
  </what-built>
  <how-to-verify>
  1. Run `npm run dev` in the project directory
  2. Open http://localhost:4321 in your browser
  3. Check the overall vibe — does it feel like a declassified CIA document meets
     psychedelic cycling, NOT a friendly cycling website?
  4. Scroll through all 5 sections — each should have dark backgrounds with subtle
     visual differentiation
  5. Look for: grain texture (subtle noise over everything), ghosted tone images,
     "Classification: Ultra" stamp, redacted text
  6. Check that headings are in a distressed typewriter font (Special Elite)
  7. Check that body text is monospaced (Space Mono)
  8. Resize browser to mobile width (~375px) — layout should still be readable
  9. Select some text — highlight should be acid-green

  Specific things to flag if wrong:
  - Fonts not loading (fallback serif/monospace visible)
  - Grain overlay blocking clicks
  - Tone images too prominent (should be ghosted, not focal)
  - Colors not matching the dark brutalist vibe
  - Any section missing or broken
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe what needs adjustment</resume-signal>
</task>

</tasks>

<verification>
1. Film-grain noise overlay covers entire viewport
2. At least 4 tone reference images copied to public/tone/
3. Hero section has CIA document background, "Classification: Ultra" stamp, redacted text
4. At least 2 sections have ghosted tone image backgrounds
5. All 5 section anchors present: hero, route, sectors, photos, info
6. `.redacted`, `.stamp`, `.classified-border`, `.tone-image` classes functional
7. The page looks like a declassified document, not a cycling website
</verification>

<success_criteria>
- Escher/CIA/surrealist visual motifs are visible on the base page
- Grain overlay adds film-grain texture across entire viewport
- Tone reference images are integrated as atmospheric design elements
- CSS utility classes (redacted, stamp, classified-border, tone-image) are available
  for reuse in all subsequent phases
- Human has visually approved the overall design direction
</success_criteria>

<output>
After completion, create `.planning/phases/02-scaffold-design-system/02-03-SUMMARY.md`
</output>
