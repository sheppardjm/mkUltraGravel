---
phase: 44-tone-image-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert-tone-images.js
  - src/pages/index.astro
  - src/components/GravelSectors.astro
  - src/components/KomSegments.astro
autonomous: false

must_haves:
  truths:
    - "The #sectors section has a tone image background visible as a subtle atmospheric texture"
    - "2 sector cards and 1 KOM card display a tone image accent blended at opacity ~0.12"
    - "Card hover states still work correctly after tone image CSS is applied"
    - "PhotoSwipe lightbox opens correctly from gallery and map markers (no z-index regression)"
    - "All tone images are static with no animation — reduced-motion requirement satisfied by default"
  artifacts:
    - path: "public/tone/square-limit-mc-escher.webp"
      provides: "Optimized WebP tone image for sectors section background"
    - path: "scripts/convert-tone-images.js"
      provides: "Pipeline entry for new tone image"
      contains: "square-limit-mc-escher"
    - path: "src/pages/index.astro"
      provides: "Tone img element inside #sectors section"
      contains: "square-limit-mc-escher.webp"
    - path: "src/components/GravelSectors.astro"
      provides: "Tone accent on first 2 sector cards with isolation: isolate"
      contains: "tone-image"
    - path: "src/components/KomSegments.astro"
      provides: "Tone accent on first KOM card with isolation: isolate"
      contains: "tone-image"
  key_links:
    - from: "scripts/convert-tone-images.js"
      to: "public/tone/square-limit-mc-escher.webp"
      via: "sharp pipeline converts source JPG to optimized WebP"
      pattern: "square-limit-mc-escher"
    - from: "src/pages/index.astro"
      to: "public/tone/square-limit-mc-escher.webp"
      via: "img src attribute"
      pattern: "src=./tone/square-limit-mc-escher\\.webp"
    - from: "src/components/GravelSectors.astro"
      to: ".tone-image class in global.css"
      via: "class attribute on accent img"
      pattern: "class=.tone-image"
    - from: "src/components/KomSegments.astro"
      to: ".tone-image class in global.css"
      via: "class attribute on accent img"
      pattern: "class=.tone-image"
---

<objective>
Add tone image coverage to the #sectors section (the only major section currently without one) and introduce subtle tone image accents inside 2 sector cards and 1 KOM card.

Purpose: Complete the atmospheric tone image layer across all page sections and add card-level texture for visual depth.
Output: Sectors section has a background tone image; 3 cards have blended tone accents with proper isolation.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tone-image-integration/44-RESEARCH.md

@src/styles/global.css (lines 160-166 — .tone-image class)
@src/pages/index.astro (lines 285-306 — #sectors section)
@src/components/GravelSectors.astro
@src/components/KomSegments.astro
@scripts/convert-tone-images.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline new tone image and add section background to #sectors</name>
  <files>
    scripts/convert-tone-images.js
    src/pages/index.astro
  </files>
  <action>
    1. Copy `images/tone/square-limit-mc-escher-1964.jpg` to `public/tone/square-limit-mc-escher-1964.jpg` (the pipeline reads from `public/tone/`, NOT `images/tone/`).

    2. In `scripts/convert-tone-images.js`, add a new entry to the `TONE_IMAGES` array:
       ```javascript
       { src: 'square-limit-mc-escher-1964.jpg', dest: 'square-limit-mc-escher.webp', width: 1000, quality: 55 },
       ```
       Place it after the existing `escharian_stairs_fb.webp` entry.

    3. Run `node scripts/convert-tone-images.js` to generate `public/tone/square-limit-mc-escher.webp`. Verify the file is created and log output shows the conversion.

    4. In `src/pages/index.astro`, inside the `<section id="sectors">` element (line 285), add a tone `<img>` as the FIRST child — before the existing `<div class="relative z-10">`. Follow the exact pattern used by every other section:
       ```astro
       <img
         src="/tone/square-limit-mc-escher.webp"
         alt=""
         class="tone-image inset-0 w-full h-full object-cover"
         loading="lazy"
       />
       ```
       The section already has `class="relative ... overflow-hidden"` and the content div already has `class="relative z-10"` — no changes needed to those.

    Do NOT use `loading="eager"` — only the hero tone image uses eager loading. Do NOT add `fetchpriority` — only the hero needs it.
  </action>
  <verify>
    - `public/tone/square-limit-mc-escher.webp` exists and is under 100KB
    - `npm run build` succeeds with no errors
    - In the built HTML, the `#sectors` section contains an `<img>` with `class="tone-image"` before the `div.relative.z-10` content wrapper
  </verify>
  <done>
    The #sectors section has a tone image background matching the pattern used by #hero, #route, #photos, and #info sections. The Escher geometric grid image is processed through the pipeline at 1000px/q55 WebP.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tone image accents to 2 sector cards and 1 KOM card</name>
  <files>
    src/components/GravelSectors.astro
    src/components/KomSegments.astro
  </files>
  <action>
    **GravelSectors.astro — accent on first 2 cards (i < 2):**

    1. On the outer card container div (currently `<div class="classified-border bg-bg-surface card-hover min-h-[280px]" data-reveal style={...}>`), merge `isolation: isolate` into the existing inline style:
       ```astro
       style={`animation-delay: ${i * 60}ms${i < 2 ? '; isolation: isolate' : ''}`}
       ```

    2. On the inner `<div class="overflow-hidden">`, add `relative` class so it becomes the positioning context for the absolute tone image:
       ```astro
       <div class={`overflow-hidden${i < 2 ? ' relative' : ''}`}>
       ```

    3. After the cover photo `<img>` (and its closing conditional), add the tone accent `<img>` — only for the first 2 cards:
       ```astro
       {i < 2 && (
         <img
           src="/tone/lsd-mind-control.webp"
           alt=""
           aria-hidden="true"
           class="tone-image inset-0 w-full h-full object-cover"
           loading="lazy"
         />
       )}
       ```
       Use `lsd-mind-control.webp` — it's already optimized (13KB) and thematically appropriate.

    4. On the `<div class="p-4">` text container, add `relative z-10` so text renders above the tone accent (only needed on accent cards, but safe to add to all):
       ```astro
       <div class={`p-4${i < 2 ? ' relative z-10' : ''}`}>
       ```

    **KomSegments.astro — accent on first card (i === 0):**

    Apply the same pattern but with `i === 0` condition:

    1. Outer card container: merge `isolation: isolate` into style for first card only:
       ```astro
       style={`animation-delay: ${i * 60}ms${i === 0 ? '; isolation: isolate' : ''}`}
       ```

    2. Inner div: add `relative` class for first card:
       ```astro
       <div class={`overflow-hidden${i === 0 ? ' relative' : ''}`}>
       ```

    3. After cover photo, add tone accent for first card:
       ```astro
       {i === 0 && (
         <img
           src="/tone/lsd-mind-control.webp"
           alt=""
           aria-hidden="true"
           class="tone-image inset-0 w-full h-full object-cover"
           loading="lazy"
         />
       )}
       ```

    4. Text container: add `relative z-10` for first card:
       ```astro
       <div class={`p-4${i === 0 ? ' relative z-10' : ''}`}>
       ```

    **Critical:** Do NOT add `isolation: isolate` to non-accent cards — it creates unnecessary stacking contexts. Do NOT animate the tone images. Do NOT set opacity/blend inline — the `.tone-image` class in `global.css` handles everything.
  </action>
  <verify>
    - `npm run build` succeeds with no errors
    - In built HTML: first 2 sector cards have `isolation: isolate` in their style attribute
    - In built HTML: first KOM card has `isolation: isolate` in its style attribute
    - In built HTML: accent cards contain an `<img>` with `class="tone-image"` and `aria-hidden="true"`
    - In built HTML: non-accent cards do NOT contain tone image markup
  </verify>
  <done>
    2 sector cards (Sandstrom Rd, Akkala Rd) and 1 KOM card (Billie Helmer) display tone image accents. The accents use the existing `.tone-image` class (opacity 0.12, lighten blend, grayscale). Card containers have `isolation: isolate` to prevent blend bleed. Text content sits above the accent via `relative z-10`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tone image background on #sectors section and tone image accents on 3 cards (2 sector + 1 KOM). All use the established .tone-image CSS class at opacity 0.12 with lighten blend mode.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:4321
    2. Scroll to the Gravel Sectors section — verify a subtle background texture is visible (Escher geometric grid at ~12% opacity)
    3. Look at the first two sector cards (Sandstrom Rd, Akkala Rd) — verify a subtle tone accent overlays the card photo area without obscuring text
    4. Look at the first KOM card (Billie Helmer) — verify the same tone accent effect
    5. Hover over accent cards — verify card-hover animation still works (no z-index or stacking regression)
    6. Click any gallery photo — verify PhotoSwipe lightbox opens correctly (not trapped behind a stacking context)
    7. Check that non-accent cards look unchanged
    8. In DevTools, enable "prefers-reduced-motion: reduce" — verify tone images are still visible but static (they should already be static since .tone-image has no animation)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `public/tone/square-limit-mc-escher.webp` exists (generated by pipeline)
- #sectors section has a tone `<img>` matching the pattern of all other sections
- 2 sector cards + 1 KOM card have tone accents with `isolation: isolate`
- Card hover states and PhotoSwipe lightbox function correctly (no z-index regression)
- All tone images are static (no animation properties)
</verification>

<success_criteria>
1. At least one tone image appears as a full-width band in the #sectors section (TONE-01)
2. 2-3 cards display a tone image accent at mix-blend-mode: lighten; opacity ~0.12 (TONE-02, TONE-03)
3. Card hover states and PhotoSwipe lightbox work correctly after changes (no stacking regression)
4. With prefers-reduced-motion: reduce active, all tone images are static (TONE-04)
</success_criteria>

<output>
After completion, create `.planning/phases/44-tone-image-integration/44-01-SUMMARY.md`
</output>
