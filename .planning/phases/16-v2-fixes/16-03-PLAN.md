---
phase: 16-v2-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - images/leaving-chatham-rock-river-rd.png
  - scripts/photo-manifest.js
  - src/components/MkUltraExplainer.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Leaving Chatham KOM card displays a photo of Rock River Rd terrain"
    - "MK Ultra explainer explains dual meaning -- CIA program AND route creator Mark Kransz's initials"
  artifacts:
    - path: "images/leaving-chatham-rock-river-rd.png"
      provides: "Source photo for Leaving Chatham KOM card"
    - path: "scripts/photo-manifest.js"
      provides: "Photo entry at mi 37.8 for Leaving Chatham range"
    - path: "src/components/MkUltraExplainer.astro"
      provides: "Updated explainer with MK = Mark Kransz connection"
  key_links:
    - from: "photo-manifest.js"
      to: "assign-card-photos.js"
      via: "mi 37.8 entry falls within Leaving Chatham range (37.6-37.98)"
      pattern: "leaving-chatham.*37\\.8"
---

<objective>
Add the user-provided photo for the Leaving Chatham KOM card and update the MK Ultra explainer to mention the dual naming meaning (CIA program + route creator Mark Kransz's initials).

Purpose: Complete the card photo coverage for Leaving Chatham KOM and add the personal naming connection to the explainer content.
Output: New photo in pipeline, updated explainer text.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@scripts/photo-manifest.js
@scripts/assign-card-photos.js
@src/components/MkUltraExplainer.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Leaving Chatham photo and update explainer content</name>
  <files>
    images/leaving-chatham-rock-river-rd.png
    scripts/photo-manifest.js
    src/components/MkUltraExplainer.astro
  </files>
  <action>
**Step 1 — Copy the user-provided photo into the images/ source directory:**
```bash
cp "/Users/Sheppardjm/Desktop/Screenshot 2026-03-27 at 8.52.01 PM.png" images/leaving-chatham-rock-river-rd.png
```

The file is a Google Street View screenshot of Rock River Rd — a paved rural road with open farmland, appropriate for the Leaving Chatham KOM area.

**Step 2 — Add photo to photo-manifest.js:**
In scripts/photo-manifest.js, add a new entry in the "Chatham area" section (after the mi 38.0 entry on line 51, before the "Post-Chatham" comment on line 53):

```js
  { filename: 'leaving-chatham-rock-river-rd.png', mi: 37.8 },
```

This positions the photo at mi 37.8, which falls WITHIN the Leaving Chatham KOM range (37.6-37.98). The assign-card-photos.js Pass 1 algorithm will now find this photo within range instead of falling back to the nearest photo.

**Step 3 — Run the prebuild pipeline to regenerate card photos:**
```bash
npm run prebuild
```

This will:
1. Copy the new .png to public/images/
2. match-photos.js picks it up from the manifest
3. assign-card-photos.js finds it within Leaving Chatham's range (37.6-37.98) and assigns it as coverPhoto
4. A new card crop is generated at public/images/cards/leaving-chatham-rock-river-rd.webp

Verify the pipeline output shows NO "WARNING: no photos within range 37.6-37.98" for Leaving Chatham (the fallback warning should disappear).

**Step 4 — Update MkUltraExplainer.astro content (Gap 4):**
In src/components/MkUltraExplainer.astro, find the third paragraph (lines 29-33) that starts with "This ride borrows the name...". Add a sentence connecting the name to route creator Mark Kransz. Replace the paragraph with:

```html
      <p>
        This ride borrows the name as a nod to the Upper Peninsula&rsquo;s own history of
        isolation and endurance &mdash; and to route creator <span class="redacted-reveal">Mark Kransz</span>,
        whose initials gave the ride its name.
        100 miles of gravel through remote forest roads will test your
        <span class="redacted-reveal">mental fortitude</span>. No mind control required &mdash;
        just legs and grit.
      </p>
```

This adds the dual-meaning explanation (MK = Mark Kransz) while keeping the existing content about endurance and grit. The name "Mark Kransz" gets the redacted-reveal treatment consistent with the existing MKULTRA and "mental fortitude" reveals.
  </action>
  <verify>
1. Run `grep 'leaving-chatham' scripts/photo-manifest.js` — should show the new entry at mi 37.8
2. Run `npm run prebuild 2>&1 | grep -i 'leaving chatham'` — should NOT show the "no photos within range" warning
3. Run `ls public/images/cards/leaving-chatham-rock-river-rd.webp` — card crop exists
4. Run `grep 'Mark Kransz' src/components/MkUltraExplainer.astro` — should find the new content
  </verify>
  <done>
Leaving Chatham KOM card displays the user-provided Rock River Rd photo (no more fallback). MK Ultra explainer explains both meanings: CIA program and Mark Kransz's initials.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. Leaving Chatham KOM card shows Rock River Rd terrain (not the previous fallback photo from mi 38)
3. MK Ultra explainer section mentions both CIA history and Mark Kransz connection
4. No prebuild pipeline warnings for Leaving Chatham
</verification>

<success_criteria>
- Leaving Chatham KOM card photo updated (Rock River Rd image)
- Pipeline assigns new photo (no fallback warning)
- Explainer text includes dual meaning (CIA + MK initials)
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/16-v2-fixes/16-03-SUMMARY.md`
</output>
