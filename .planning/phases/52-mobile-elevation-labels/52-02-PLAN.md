---
phase: 52-mobile-elevation-labels
plan: 02
type: execute
wave: 1
depends_on: ["52-01"]
files_modified: ["src/components/ElevationProfile.astro"]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All sector name labels render normally on 640px+ viewports, including narrow sectors like Down Jeep"
    - "Narrow sectors show both name and stars, rotated -90 degrees"
  artifacts:
    - path: "src/components/ElevationProfile.astro"
      provides: "Sector annotation labels with name included for all sectors"
      contains: "[sector.name, starsStr]"
  key_links:
    - from: "labelContent assignment"
      to: "label.content"
      via: "isNarrow branch must still include sector.name"
      pattern: "isNarrow.*sector\\.name"
---

<objective>
Fix missing sector name on narrow sectors (Down Jeep) in the elevation profile chart.

Purpose: The isNarrow guard strips the sector name from labelContent when width < 1.0mi, leaving only star characters. Down Jeep (0.59mi) loses its name. Since the -90 degree rotation already handles horizontal constraint and Chart.js annotation plugin does not clip labels to their box, the name can safely be included.

Output: Updated ElevationProfile.astro where narrow sectors show both name and stars (rotated), identical to wide sectors except for the rotation.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-mobile-elevation-labels/52-01-SUMMARY.md
@src/components/ElevationProfile.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include sector name in narrow-sector labelContent</name>
  <files>src/components/ElevationProfile.astro</files>
  <action>
In src/components/ElevationProfile.astro, lines 64-68, change the labelContent assignment so that narrow sectors include both sector.name and starsStr, not just starsStr alone.

Current code (lines 64-68):
```
const isNarrow = widthMi < 1.0;
const starsStr = '\u2605'.repeat(sector.stars);
const labelContent: string[] = isNarrow
  ? [starsStr]
  : [sector.name, starsStr];
```

Change to:
```
const isNarrow = widthMi < 1.0;
const starsStr = '\u2605'.repeat(sector.stars);
const labelContent: string[] = [sector.name, starsStr];
```

The isNarrow variable is still used on line 85 for the rotation (-90 vs 0), so keep the isNarrow declaration. Only the labelContent ternary changes — it becomes unconditional since both branches now produce the same array.

Do NOT change: the isNarrow variable declaration, the rotation logic, the display function, or any other annotation properties.
  </action>
  <verify>
1. Run `npm run build` — build succeeds with no errors.
2. Visual inspection of the changed lines: labelContent is always [sector.name, starsStr] for all sectors.
3. Confirm isNarrow is still declared and used for rotation on line 85.
  </verify>
  <done>All sectors (including Down Jeep at 0.59mi) include both the sector name and star rating in their label content. Narrow sectors still rotate -90 degrees. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. The labelContent assignment no longer conditionally strips sector.name
3. The isNarrow variable still controls rotation (-90 for narrow, 0 for wide)
4. No other annotation properties were modified
</verification>

<success_criteria>
- Down Jeep sector label shows both name and stars on 640px+ viewports
- All other sector labels unchanged in behavior
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/52-mobile-elevation-labels/52-02-SUMMARY.md`
</output>
