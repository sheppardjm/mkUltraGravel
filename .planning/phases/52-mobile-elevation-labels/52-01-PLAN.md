---
phase: 52-mobile-elevation-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ElevationProfile.astro]
autonomous: true

must_haves:
  truths:
    - "On a 375px viewport, the elevation profile shows no text labels — only colored annotation bands and the elevation line"
    - "On a 640px+ viewport, all sector name labels, star-rating labels, and KOM segment labels render normally"
    - "At 639px labels are hidden; at 640px labels are visible — precise breakpoint"
    - "Resizing the browser across the 640px boundary does not break the chart — annotation bands remain visible at all sizes"
  artifacts:
    - path: "src/components/ElevationProfile.astro"
      provides: "Responsive label visibility via scriptable display function"
      contains: "window.innerWidth >= 640"
  key_links:
    - from: "label.display function"
      to: "Chart.js responsive re-render"
      via: "scriptable option evaluated on afterUpdate lifecycle hook"
      pattern: "display.*window\\.innerWidth.*640"
---

<objective>
Hide elevation profile chart labels on mobile viewports (below 640px) while preserving colored annotation bands at all sizes.

Purpose: The elevation profile is unreadable on mobile because sector names, star ratings, and KOM segment names create dense label clutter. Hiding labels below 640px makes the chart clean on mobile while keeping full detail on desktop.
Output: Updated ElevationProfile.astro with scriptable `label.display` functions on both sector and KOM annotation blocks.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-mobile-elevation-labels/52-RESEARCH.md
@src/components/ElevationProfile.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make annotation labels responsive to viewport width</name>
  <files>src/components/ElevationProfile.astro</files>
  <action>
In `src/components/ElevationProfile.astro`, make two targeted replacements in the `<script>` block:

1. **Sector annotation labels** (around line 79, inside the `sectors.forEach` loop):
   Change `display: true` to `display: () => window.innerWidth >= 640`

   Before:
   ```javascript
   label: {
     display: true,
     content: labelContent,
   ```
   After:
   ```javascript
   label: {
     display: () => window.innerWidth >= 640,
     content: labelContent,
   ```

2. **KOM annotation labels** (around line 107, inside the `annotations.kom.forEach` loop):
   Change `display: true` to `display: () => window.innerWidth >= 640`

   Before:
   ```javascript
   label: {
     display: true,
     content: kom.name,
   ```
   After:
   ```javascript
   label: {
     display: () => window.innerWidth >= 640,
     content: kom.name,
   ```

CRITICAL constraints:
- Only change `label.display`, NOT the annotation-root `display`. The annotation-root `display` controls the entire box (band + label). Setting it to false would hide the colored bands, violating ELEV-08.
- Use `window.innerWidth >= 640` (not `> 639`). This matches the success criteria wording: "at exactly 640px, labels are visible."
- Do NOT add any resize event listeners or call `chart.update()` manually. Chart.js `responsive: true` (already configured) automatically triggers re-renders on window resize, which re-evaluates the scriptable function.
- Do NOT touch any other properties in the label blocks — content, position, color, font, yAdjust, rotation must remain unchanged.
  </action>
  <verify>
1. Run `grep -n 'display:.*window.innerWidth' src/components/ElevationProfile.astro` — should return exactly 2 matches (one in sector loop, one in KOM loop).
2. Run `grep -n 'display: true' src/components/ElevationProfile.astro` — should return 0 matches (both instances replaced).
3. Run `npm run build` (or the project's build command) — should complete without errors.
  </verify>
  <done>
Both sector and KOM annotation label blocks use `display: () => window.innerWidth >= 640` instead of `display: true`. No other lines changed. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'window.innerWidth >= 640' src/components/ElevationProfile.astro` returns `2`
2. `grep -c 'display: true' src/components/ElevationProfile.astro` returns `0` (within the annotation label blocks)
3. Build completes without errors
4. The annotation-root `display` property is NOT present or NOT modified — only `label.display` was changed
</verification>

<success_criteria>
- Exactly 2 lines changed in ElevationProfile.astro (both `display: true` replaced with scriptable function)
- No new files created, no new dependencies added
- Build passes cleanly
- Colored annotation bands (backgroundColor, borderColor) remain unconditionally visible (not gated by viewport width)
</success_criteria>

<output>
After completion, create `.planning/phases/52-mobile-elevation-labels/52-01-SUMMARY.md`
</output>
