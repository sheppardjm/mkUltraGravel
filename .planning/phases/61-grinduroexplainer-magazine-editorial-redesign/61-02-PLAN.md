---
phase: 61-grinduroexplainer-magazine-editorial-redesign
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified: [src/components/GrinduroExplainer.astro, src/styles/global.css]
autonomous: true

must_haves:
  truths:
    - "The opening paragraph ('This isn't a time trial...') has a drop cap on the letter T using ::first-letter in Special Elite font at approximately 3.8em, colored accent-green"
    - "A pull quote reading 'Race the sectors. Suffer together.' appears as a blockquote between paragraph 2 and the second image break, with a left border in accent-green and Special Elite italic text"
    - "All paragraphs, image breaks, and the pull quote animate in on scroll using the existing data-reveal pattern"
    - "The drop cap text wraps correctly — no large gap between the enlarged T and the first line of body text"
    - "Scroll-reveal works without any changes to index.astro — existing IntersectionObserver picks up the new data-reveal elements"
  artifacts:
    - path: "src/components/GrinduroExplainer.astro"
      provides: "Drop cap paragraph, pull quote blockquote, data-reveal attributes on all content blocks"
      contains: "explainer-drop-cap"
    - path: "src/styles/global.css"
      provides: "Drop cap ::first-letter rule and pull-quote styling"
      contains: "explainer-drop-cap"
  key_links:
    - from: "GrinduroExplainer.astro data-reveal attributes"
      to: "index.astro IntersectionObserver"
      via: "querySelectorAll('[data-reveal]') picks up all elements with the attribute"
      pattern: "data-reveal"
    - from: ".explainer-drop-cap::first-letter in global.css"
      to: "paragraph 1 class='explainer-drop-cap' in GrinduroExplainer.astro"
      via: "CSS class selector matches Astro component element"
      pattern: "explainer-drop-cap"
---

<objective>
Add drop cap typography, pull quote, and scroll-reveal animations to the GrinduroExplainer editorial layout built in Plan 01.

Purpose: The grid structure from Plan 01 provides the magazine layout skeleton. This plan adds the editorial polish — a drop cap opening, a highlighted pull quote for rhythm, and scroll-reveal entrance animations that match the rest of the site.
Output: Updated GrinduroExplainer.astro with drop cap class, pull quote blockquote, and data-reveal attributes; global.css with drop cap and pull quote CSS rules.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-grinduroexplainer-magazine-editorial-redesign/61-RESEARCH.md
@.planning/phases/61-grinduroexplainer-magazine-editorial-redesign/61-01-SUMMARY.md
@src/components/GrinduroExplainer.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drop cap and pull quote CSS rules to global.css</name>
  <files>src/styles/global.css</files>
  <action>
In `src/styles/global.css`, add the following rules inside `@layer base`, after the `.full-bleed` rule added by Plan 01:

```css
  .explainer-drop-cap::first-letter {
    float: left;
    font-family: var(--font-display);
    font-size: 3.8em;
    line-height: 0.85;
    margin-right: 0.1em;
    color: var(--color-accent-green);
    padding-right: 0.05em;
  }

  .pull-quote {
    border-left: 3px solid var(--color-accent-green);
    padding-left: 1.25em;
    margin: 1.5em 0;
    font-family: var(--font-display);
    font-size: 1.1em;
    color: var(--color-accent-white);
    font-style: italic;
    line-height: 1.4;
  }
```

CRITICAL constraints:
- Use `float: left` for the drop cap, NOT `initial-letter` (not Baseline — limited browser support).
- `line-height: 0.85` on `::first-letter` prevents the enlarged letter from pushing down the first line of body text.
- Use CSS custom properties (`var(--font-display)`, `var(--color-accent-green)`, `var(--color-accent-white)`) — do NOT hardcode values.
- Place these in `@layer base`, NOT in `@layer utilities`.
  </action>
  <verify>
1. `grep -c 'explainer-drop-cap' src/styles/global.css` — returns `1` (the ::first-letter rule)
2. `grep -c 'pull-quote' src/styles/global.css` — returns `1` (the class definition)
3. `grep 'float: left' src/styles/global.css` — returns the drop cap float rule
4. `grep 'initial-letter' src/styles/global.css` — returns nothing (NOT used)
  </verify>
  <done>
global.css contains `.explainer-drop-cap::first-letter` with float:left, Special Elite font, 3.8em size, accent-green color, and 0.85 line-height. `.pull-quote` class has accent-green left border, Special Elite italic, accent-white color.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add drop cap class, pull quote blockquote, and data-reveal attributes to GrinduroExplainer</name>
  <files>src/components/GrinduroExplainer.astro</files>
  <action>
Update `src/components/GrinduroExplainer.astro` (built by Plan 01) to add three features:

**1. Drop cap on first paragraph:**
Add the `explainer-drop-cap` class to the first `<p>` element (the one starting with "This isn't a time trial..."):
```html
<p class="explainer-drop-cap text-text-body text-sm leading-relaxed">
```

**2. Pull quote blockquote between paragraph 2 and the second image break:**
Insert a `<blockquote>` after paragraph 2 (the sectors/KOM paragraph) and BEFORE the second full-bleed image div:
```html
<blockquote class="pull-quote" data-reveal>
  Race the sectors. Suffer together.
</blockquote>
```
This text is duplicated from paragraph 3 intentionally — pull quotes repeat key phrases for emphasis. The text also remains in paragraph 3.

**3. data-reveal attributes on all content blocks:**
Add the `data-reveal` attribute to ALL direct children of the grid section:
- The "Grinduro Format" label `<p>` — add `data-reveal`
- The drop cap paragraph — add `data-reveal`
- The first full-bleed div — add `data-reveal`
- Paragraph 2 (sectors/KOM) — add `data-reveal`
- The pull quote `<blockquote>` — already has `data-reveal` (from step 2)
- The second full-bleed div — add `data-reveal`
- Paragraph 3 — add `data-reveal`

The final component structure should be:
```
<section class="explainer-grid py-8 mb-8">
  <p data-reveal>           ← "Grinduro Format" label
  <p data-reveal>           ← paragraph 1 with drop cap
  <div data-reveal>         ← full-bleed image 1
  <p data-reveal>           ← paragraph 2
  <blockquote data-reveal>  ← pull quote
  <div data-reveal>         ← full-bleed image 2
  <p data-reveal>           ← paragraph 3
</section>
```

CRITICAL constraints:
- Do NOT change the text content of any paragraph — all three paragraphs remain word-for-word identical to the original.
- The pull quote text is exactly "Race the sectors. Suffer together." — no other text.
- Do NOT add `data-reveal` to the `<section>` root element — only to direct children.
- Do NOT modify the full-bleed div structure, img src, or filter styles from Plan 01.
- Do NOT modify index.astro — the existing IntersectionObserver automatically picks up new `[data-reveal]` elements.
  </action>
  <verify>
1. `grep -c 'explainer-drop-cap' src/components/GrinduroExplainer.astro` — returns `1` (on first paragraph)
2. `grep -c 'pull-quote' src/components/GrinduroExplainer.astro` — returns `1` (blockquote class)
3. `grep -c 'data-reveal' src/components/GrinduroExplainer.astro` — returns `7` (all 7 direct children)
4. `grep 'Race the sectors' src/components/GrinduroExplainer.astro` — returns 2 matches (pull quote + paragraph 3)
5. `grep -c 'blockquote' src/components/GrinduroExplainer.astro` — returns `2` (open + close tags)
6. Build completes without errors: `npx astro build` or equivalent
  </verify>
  <done>
GrinduroExplainer.astro has: (1) `explainer-drop-cap` class on the opening paragraph, (2) a `<blockquote class="pull-quote">` between paragraph 2 and image break 2, and (3) `data-reveal` attributes on all 7 direct children of the grid section. No index.astro changes needed. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'explainer-drop-cap' src/styles/global.css` returns `1`
2. `grep -c 'pull-quote' src/styles/global.css` returns `1`
3. `grep -c 'explainer-drop-cap' src/components/GrinduroExplainer.astro` returns `1`
4. `grep -c 'pull-quote' src/components/GrinduroExplainer.astro` returns `1`
5. `grep -c 'data-reveal' src/components/GrinduroExplainer.astro` returns `7`
6. `grep 'initial-letter' src/styles/global.css` returns nothing
7. `grep 'float: left' src/styles/global.css` returns the drop cap rule
8. Build completes without errors
</verification>

<success_criteria>
- Opening paragraph has a visually prominent drop cap (T) in Special Elite font, accent-green color, ~3.8em
- Pull quote "Race the sectors. Suffer together." appears between paragraph 2 and second image break with accent-green left border
- All 7 content blocks (label, 3 paragraphs, 2 image breaks, 1 pull quote) have data-reveal and animate in on scroll
- No changes to index.astro required
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/61-grinduroexplainer-magazine-editorial-redesign/61-02-SUMMARY.md`
</output>
