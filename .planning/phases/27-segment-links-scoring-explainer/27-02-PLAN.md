---
phase: 27-segment-links-scoring-explainer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ScoringExplainer.astro
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "A scoring explainer section describes Gravel Champion scoring (cumulative time across 6 sectors, lowest wins) clearly enough for a first-time visitor"
    - "The scoring explainer describes KOM/QOM Champion scoring (10-1 points for top 10 per climb, 3 climbs, 30 max, most points wins)"
    - "Both scoring formats mention gender categories (men, women, non-binary)"
    - "Powered by Strava attribution with #FC5200 orange is visible in the sectors section"
  artifacts:
    - path: "src/components/ScoringExplainer.astro"
      provides: "Scoring system explainer component (SCORE-03)"
      min_lines: 15
    - path: "src/pages/index.astro"
      provides: "ScoringExplainer import + Powered by Strava attribution"
      contains: "ScoringExplainer"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/ScoringExplainer.astro"
      via: "Astro component import and render"
      pattern: "import ScoringExplainer"
---

<objective>
Create a ScoringExplainer component explaining both Gravel Champion and KOM/QOM Champion scoring formats, integrate it into the sectors section of index.astro, and add "Powered by Strava" attribution.

Purpose: Fulfills SCORE-03 (scoring system explainer on site) and the "Powered by Strava" attribution success criteria.
Output: New ScoringExplainer.astro component and updated index.astro with the component import + Strava attribution.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-segment-links-scoring-explainer/27-RESEARCH.md
@src/components/GrinduroExplainer.astro
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScoringExplainer.astro component</name>
  <files>src/components/ScoringExplainer.astro</files>
  <action>
Create a new file `src/components/ScoringExplainer.astro` following the exact same structural pattern as `GrinduroExplainer.astro` (classified-border, p-6 md:p-8, mb-8, text-text-body, text-sm, leading-relaxed, space-y-3).

Content must explain both scoring formats clearly for a first-time visitor:

1. **Section label**: "Scoring System" — using `text-text-muted text-xs uppercase tracking-widest mb-4 !mt-0` (same as GrinduroExplainer's "Grinduro Format" label)

2. **Gravel Champion**: Explain that the lowest cumulative elapsed time across all 6 timed gravel sectors wins. Use `text-accent-white` for "Gravel Champion" strong text. Mention gender categories: men, women, non-binary.

3. **KOM/QOM Champion**: Explain the top-10 points system — 10 points for 1st, 9 for 2nd, down to 1 point for 10th, across 3 climbs (30 points max). Most total points wins. Use `text-accent-green` for "KOM/QOM Champion" strong text. Mention gender categories.

4. **Submission note**: Brief mention that results are submitted via Strava activity link after the event, and full leaderboards will be published on the site.

Keep the tone consistent with GrinduroExplainer — direct, slightly irreverent, cycling-literate. No corporate language.

Full component (use this exactly):
```astro
---
// ScoringExplainer.astro — Grinduro scoring system explainer (SCORE-03)
---
<div class="classified-border p-6 md:p-8 mb-8 text-text-body text-sm leading-relaxed space-y-3">
  <p class="text-text-muted text-xs uppercase tracking-widest mb-4 !mt-0">Scoring System</p>
  <p>
    <strong class="text-accent-white">Gravel Champion</strong> &mdash;
    lowest cumulative elapsed time across all 6 timed sectors wins.
    Gender categories: men, women, non-binary.
  </p>
  <p>
    <strong class="text-accent-green">KOM/QOM Champion</strong> &mdash;
    top-10 points per climb (10&ndash;1). Three climbs. 30 points max.
    Most total points wins. Gender categories: men, women, non-binary.
  </p>
  <p>
    Results submitted via Strava activity link after the event.
    Full leaderboards published here once submissions close.
  </p>
</div>
```
  </action>
  <verify>
Verify file exists and has correct structure:
`cat src/components/ScoringExplainer.astro`

Expected: Component file with classified-border container, three paragraphs explaining scoring, matches GrinduroExplainer pattern.
  </verify>
  <done>ScoringExplainer.astro exists with clear explanations of both Gravel Champion and KOM/QOM Champion scoring formats including gender categories.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ScoringExplainer and Powered by Strava into index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
Make two changes to `src/pages/index.astro`:

1. **Add import** — In the frontmatter import block (after the GrinduroExplainer import on line 19), add:
```typescript
import ScoringExplainer from "../components/ScoringExplainer.astro";
```

2. **Place ScoringExplainer** — In the `#sectors` section, add `<ScoringExplainer />` immediately after `<GrinduroExplainer />` (line 287). The scoring explainer extends the format explanation with specific scoring details, so it belongs right after the format explainer and before the card grid.

3. **Add "Powered by Strava" attribution** — At the bottom of the `#sectors` section's `<div class="relative z-10">`, after the closing `</div>` of the `grid md:grid-cols-3` container (line 299) and before the closing `</div>` of `relative z-10`, add:
```astro
        <p class="text-text-muted text-xs mt-8 text-right">
          Powered by <span class="text-[#FC5200] font-bold">Strava</span>
        </p>
```

This places the attribution below the card grid, right-aligned, using the muted label style with Strava orange on the word "Strava".

IMPORTANT: Do NOT modify any other part of index.astro. Use targeted Edit tool edits only.
  </action>
  <verify>
Run: `PATH="/usr/local/opt/node@25/bin:$PATH" npx astro build 2>&1 | tail -20`

Expected: Build succeeds with no errors.

Then verify:
- `grep "ScoringExplainer" dist/index.html` — confirms component rendered
- `grep "Powered by" dist/index.html` — confirms attribution present
- `grep "Gravel Champion" dist/index.html` — confirms scoring text rendered
- `grep "KOM/QOM Champion" dist/index.html` — confirms both formats explained
  </verify>
  <done>index.astro imports and renders ScoringExplainer after GrinduroExplainer. "Powered by Strava" attribution appears at bottom of sectors section with #FC5200 orange. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `PATH="/usr/local/opt/node@25/bin:$PATH" npx astro build` completes without errors
2. `grep "Gravel Champion" dist/index.html` returns match
3. `grep "KOM/QOM Champion" dist/index.html` returns match
4. `grep "Powered by" dist/index.html` returns match with FC5200 color
5. `grep "men, women, non-binary" dist/index.html` confirms gender categories mentioned
6. ScoringExplainer appears after GrinduroExplainer and before the card grid in the rendered HTML
</verification>

<success_criteria>
- ScoringExplainer.astro component exists and explains both scoring formats
- Both formats mention gender categories (men, women, non-binary)
- Component renders in #sectors section after GrinduroExplainer
- "Powered by Strava" attribution visible with #FC5200 orange color
- Astro build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-segment-links-scoring-explainer/27-02-SUMMARY.md`
</output>
